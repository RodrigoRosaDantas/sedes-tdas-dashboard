import fs from 'node:fs/promises';
import path from 'node:path';
const ROOT=process.cwd(),BASE='/sedes-tdas-dashboard/',read=file=>fs.readFile(path.join(ROOT,file),'utf8'),exists=file=>fs.access(path.join(ROOT,file)).then(()=>true).catch(()=>false),required=(condition,message)=>{if(!condition)throw new Error(`PWA da execução diária: ${message}`)};
const sw=await read('sw.js'),postprocess=await read('scripts/postprocess-v26.mjs'),preserve=await read('scripts/preserve-v27-pwa.mjs'),versionSync=await read('scripts/sync-platform-version.mjs'),common=await read('assets/common.js'),manifest=JSON.parse(await read('manifest.webmanifest')),packageData=JSON.parse(await read('package.json')),platformVersion=JSON.parse(await read('data/platform-version.json'));
const list=name=>{const match=sw.match(new RegExp(`const ${name}=\\[([\\s\\S]*?)\\];`));required(match,`lista ${name} ausente`);return[...match[1].matchAll(/(['"])(.*?)\1/g)].map(item=>item[2])};
const routes=list('CORE_ROUTES'),assets=list('ASSETS'),data=list('DATA'),version=sw.match(/const VERSION=['"]([^'"]+)['"]/u)?.[1]||'',visualCacheRev=versionSync.match(/const VISUAL_CACHE_REV=['"]([^'"]+)['"]/u)?.[1]||'';
const requiredRoutes=['configuracoes/','dados-locais/','estudar/','resolver/','revisar/','caderno-erros/','desempenho/','fila-ia/','mentor/'];
const requiredAssets=['assets/home-mobile.js','assets/tdas-mobile-ux.js','assets/tdas-mobile-ux.css','assets/tdas-pro-dashboard.css','assets/home-mobile-hotfix.css','assets/tdas-pro-modules.css','assets/tdas-pro-modules.js','assets/tdas-command-palette.css','assets/tdas-command-palette.js','assets/mentor.js','assets/mentor.css','assets/settings.js','assets/integration/daily-execution.js','assets/integration/daily-law.js','assets/integration/today-execution.js','assets/integration/daily-question-page.js','assets/integration/daily-progress.js','assets/integration/local-backup.js','assets/integration/daily-content.css','assets/integration/command-center.css','assets/integration/home-command-center.js','assets/integration/audit-unlinked-errors.js','assets/integration/session-draft.js','assets/integration/module-dashboard.js','assets/integration/module-player.js','assets/integration/module-store.js','assets/integration/module-reviews.js','assets/integration/module-error-book.js','assets/integration/module-performance.js','assets/integration/module-ai-queue.js','assets/integration/player-core.js','assets/integration/player.css','assets/integration/study-ux.js','assets/integration/mentor-engine.js','assets/integration/mentor-ux.js','assets/dashboard-pro-2026.css','assets/integration/home-dashboard-pro-2026.js'];
const requiredData=['data/integration/navigation.json','data/integration/question-catalog.json','data/integration/daily-execution.json','data/integration/daily-material.json','data/platform-version.json'];
required(version===platformVersion.serviceWorkerVersion,`versão do cache diverge do manifesto: ${version}`);
for(const route of requiredRoutes){required(routes.includes(route),`rota fora do cache: ${route}`);required(await exists(`${route}index.html`),`rota ausente: ${route}`);required(postprocess.includes(`\"${route}\"`),`gerador pode remover a rota: ${route}`)}
for(const asset of requiredAssets){required(assets.includes(asset),`asset fora do cache: ${asset}`);required(await exists(asset),`asset ausente: ${asset}`);required(postprocess.includes(`\"${asset}\"`),`gerador pode remover o asset: ${asset}`)}
for(const file of requiredData){required(data.includes(file),`dado fora do cache: ${file}`);required(await exists(file),`dado ausente: ${file}`);if(file==='data/platform-version.json')required(versionSync.includes(file),'sincronizador pode remover o manifesto de versão');else required(postprocess.includes(`\"${file}\"`),`gerador pode remover o dado: ${file}`)}
required(!data.some(file=>file.includes('question-keys/'))&&!sw.includes('question-keys/'),'correção foi pré-carregada antes da finalização');
required(versionSync.includes("replaceConstant(sw,'VERSION'")&&versionSync.includes("ensureArrayEntry(sw,'DATA','data/platform-version.json')"),'sincronizador não governa versão e precache');
required(/pro10$/u.test(visualCacheRev),`revisão visual do cache deve permanecer na geração PRO10, permitindo revisões de invalidação: ${visualCacheRev||'ausente'}`);
required(sw.includes('caches.match(request,{ignoreSearch:true})')&&sw.includes("if(url.search||url.pathname.includes('/data/'))"),'service worker não possui fallback offline para URLs versionadas');
required(postprocess.includes('caches.match(request,{ignoreSearch:true})')&&postprocess.includes("if(url.search||url.pathname.includes('/data/'))"),'gerador pode remover o fallback offline de URLs versionadas');
required(sw.includes("new Request(request,{cache:'no-store'})")&&sw.includes('fetchAndCache(event.request,{fresh:true})'),'service worker não força rede fresca para navegação e recursos versionados');
required(preserve.includes('preserveFreshNetwork')&&preserve.includes("cache:'no-store'")&&preserve.includes('fresh:true'),'preservador pode remover a navegação fresca após a sincronização automática');
required(common.includes("navigator.serviceWorker.register(script,{updateViaCache:'none'})"),'shell não solicita a versão mais recente do service worker');
required(!common.includes('registration.update()'),'shell não deve repetir a atualização do service worker e gerar erro no navegador');
required(!data.some(file=>/pe76|pilot/i.test(file))&&!assets.some(file=>/pilot-catalog|real-study|pe-pilot-status/i.test(file)),'PWA ainda inclui conteúdo de exemplo.');
required(manifest.start_url===BASE&&manifest.scope===BASE,'escopo do manifesto divergente');
required(packageData.scripts?.['check:pwa']==='node scripts/validate-pwa-integration.mjs','comando check:pwa ausente');
console.log(`PWA validado: ${requiredRoutes.length} rotas críticas, ${requiredAssets.length} módulos, versão ${version}, geração visual ${visualCacheRev}, atualização imediata e fallback offline ativos, Dashboard PRO, Mentor e hotfix mobile disponíveis offline, correção fora do precache.`);
await import('./audit-site-integrity.mjs');
