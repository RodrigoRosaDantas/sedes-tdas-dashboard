import assert from 'node:assert/strict';
import fs from 'node:fs/promises';
import {createSession, evaluateSession, selectAnswer} from '../assets/integration/player-core.js';
import {saveCompletedAttempt} from '../assets/integration/module-store.js';
import {DAILY_STORAGE_KEY,DRAFT_STORAGE_KEY,MODULE_STORAGE_KEY,createLocalBackup,restoreLocalBackup} from '../assets/integration/local-backup.js';

class MemoryStorage {constructor(entries = {}) { this.map = new Map(Object.entries(entries)); }getItem(key) { return this.map.has(key) ? this.map.get(key) : null; }setItem(key, value) { this.map.set(key, String(value)); }removeItem(key) { this.map.delete(key); }}

// 1) Aprendizado EDAS preservado no TDAS: item editorialmente suspeito não vira erro real nem revisão interna.
const catalog={catalogId:'convergence-editorial-quarantine',peId:'PE89',questions:[{id:'q-annul',numeroOriginal:1,materia:'Teste',assunto:'Integridade editorial',subassunto:'Anulação',enunciado:'Q1',alternativas:{A:'a',B:'b',C:'c',D:'d',E:'e'}},{id:'q-source',numeroOriginal:2,materia:'Teste',assunto:'Integridade editorial',subassunto:'Fonte',enunciado:'Q2',alternativas:{A:'a',B:'b',C:'c',D:'d',E:'e'}}]};
const answerKey={material_id:catalog.catalogId,answers:[{id:'q-annul',gabarito:'A'},{id:'q-source',gabarito:'B'}]};let session=createSession({id:catalog.catalogId,questoes:catalog.questions},1_000);session=selectAnswer(session,'q-annul','C',2_000);session=selectAnswer(session,'q-source','D',3_000);const evaluation=evaluateSession(session,answerKey,10_000),quarantineStorage=new MemoryStorage(),quarantined=saveCompletedAttempt({catalog,evaluation,responseMeta:{'q-annul':{issue:'annulment_pending'},'q-source':{issue:'source_error'}}},quarantineStorage);
assert.equal(quarantined.state.errors.length,0);assert.equal(quarantined.state.aiQueue.length,2);assert.equal(quarantined.state.reviews.length,0);assert.deepEqual(new Set(quarantined.state.aiQueue.map(item=>item.classification)),new Set(['annulment_pending','source_error']));assert.equal(quarantined.attempt.localOnly,true);assert.equal(quarantined.attempt.persistent,false);assert.equal(quarantined.attempt.cloudSync,false);assert.equal(quarantined.attempt.notionWriteback,false);assert.equal(quarantineStorage.getItem(MODULE_STORAGE_KEY),null,'Tentativa concluída não pode virar histórico local persistente.');

// 2) Backup local é transacional, mas limitado a progresso operacional + sessão ativa.
const daily={version:1,items:{PE89:{material:true,questions:true,registered:false,updatedAt:'2026-08-15T12:00:00-03:00'}}};
const draft={schemaVersion:'1.0.0',catalogId:'tdas-test',peId:'PE89',savedAt:1,session:{schemaVersion:'1.0.0',materialId:'tdas-test',questionIds:['q1'],answers:{q1:'A'},currentIndex:0,startedAt:1,updatedAt:2,finishedAt:null},responseMeta:{}};
const sourceStorage=new MemoryStorage({[DAILY_STORAGE_KEY]:JSON.stringify(daily),[DRAFT_STORAGE_KEY]:JSON.stringify(draft),[MODULE_STORAGE_KEY]:JSON.stringify({schemaVersion:'2.0.0',attempts:[{id:'legado'}],errors:[],marked:[],reviews:[],aiQueue:[]})});const backup=createLocalBackup(sourceStorage,'2026-08-15T12:00:00-03:00');assert.equal('questionModule' in backup.stores,false);assert.equal(backup.summary.activeSession,true);
class FailingStorage extends MemoryStorage {constructor(entries={}){super(entries);this.failDraftWrite=false}setItem(key,value){if(this.failDraftWrite&&key===DRAFT_STORAGE_KEY)throw new Error('falha simulada');super.setItem(key,value)}}
const previousDaily=JSON.stringify({version:1,items:{PE88:{material:true,questions:false,registered:false,updatedAt:null}}}),previousDraft=JSON.stringify({...draft,catalogId:'anterior'}),failing=new FailingStorage({[DAILY_STORAGE_KEY]:previousDaily,[DRAFT_STORAGE_KEY]:previousDraft});failing.failDraftWrite=true;assert.throws(()=>restoreLocalBackup(backup,failing),/restauração foi revertida/i);assert.equal(failing.getItem(DAILY_STORAGE_KEY),previousDaily);assert.equal(failing.getItem(DRAFT_STORAGE_KEY),previousDraft);

// 3) Desempenho pessoal foi aposentado; evolução oficial continua explicitamente separada.
const performanceUi=await fs.readFile('assets/integration/module-performance-v6.js','utf8');assert.match(performanceUi,/Resultados oficiais, não histórico do navegador/);assert.match(performanceUi,/desempenho consolidado continua vindo das fontes oficiais sincronizadas/);assert.doesNotMatch(performanceUi,/private-history|firebase/i);

// 4) Diferença intencional: o TDAS é diário. Um "no_changes" recente não pode tornar válido o snapshot de outro dia.
process.env.MONITOR_SELF_TEST='true';const{evaluatePublication}=await import('./monitor-tdas-publication.mjs?convergence=1');delete process.env.MONITOR_SELF_TEST;const now=new Date('2026-08-15T12:00:00-03:00'),syncAt='2026-08-15T11:30:00-03:00',staleDailySnapshot=evaluatePublication({now,platform:{syncAt,peId:'PE89'},today:{meta:{snapshotDate:'2026-08-14',examDate:'2026-09-06'},current:{date:'2026-08-14',pe:'PE89',meta:2}},agenda:{meta:{examDate:'2026-09-06'},current:{pe:'PE89',date:'2026-08-15',title:'PE diário',planned_questions:'2'},next:[],allFuture:[]},catalog:{peId:'PE89',questionCount:2,questions:[{id:'q1'},{id:'q2'}]},material:{mode:'notion-daily-material',peId:'PE89',html:'x'.repeat(300),source:{pageId:'material-page'}},contract:{current:{peId:'PE89',materialPageId:'material-page'}},history:{entries:[{at:syncAt,status:'no_changes'}]},maxAgeMinutes:180});assert.equal(staleDailySnapshot.status,'blocked');assert.ok(staleDailySnapshot.issues.some(item=>item.code==='SNAPSHOT_DATE_STALE'));assert.ok(staleDailySnapshot.issues.some(item=>item.code==='EXECUTION_DATE_STALE'));assert.ok(!staleDailySnapshot.issues.some(item=>item.code==='SYNC_STALE'));
console.log('Convergência EDAS → TDAS protegida: quarentena editorial, backup mínimo local-only, separação oficial e frescor diário específico do TDAS.');
