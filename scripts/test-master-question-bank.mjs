import assert from 'node:assert/strict';
import {buildTdasSnapshot,selectPublishedMaterials} from './sync-tdas-master-question-bank.mjs';
const mc=(n,gabarito='A')=>({id:`legacy-${n}`,codigo:`Q${n}`,numero_original:n,disciplina:'Português',assunto:'Assunto',subassunto:'Sub',texto_base:null,enunciado:`Enunciado ${n}`,alternativas:{A:'A1',B:'B1',C:'C1',D:'D1',E:'E1'},gabarito,comentario:'Comentário',fundamento:'Fundamento',pegadinha:'Pegadinha'});
const ce=(n,gabarito='Certo')=>({id:`ce-${n}`,codigo:`CE${n}`,numero_original:n,disciplina:'Português',assunto:'Assunto CE',enunciado:`Item ${n}`,alternativas:{Certo:'Certo',Errado:'Errado'},gabarito,comentario:'Comentário CE'});
const tdas={id:'tdas-material',tipo_material:'simulado',fonte:'Instituto Quadrix',nome:'TDAS material',ano:2026,orgao:'SEDES/DF',cargo:'TDAS — Técnico Administrativo',codigo_cargo:'202',disciplina:'Português',status:'publicado',quantidade_questoes:2,questoes:[mc(1,'B'),mc(2,'E')]};
const outro={id:'outra-prova',tipo_material:'prova',fonte:'Instituto Quadrix',nome:'Outra prova',ano:2026,orgao:'Conselho',cargo:'Auxiliar Administrativo',codigo_cargo:'200',disciplina:'Múltiplas matérias',status:'publicado',quantidade_questoes:2,questoes:[ce(1,'Certo'),ce(2,'Errado')]};
const catalog={release_version:'x',exported_at:'2026-08-14T00:00:00Z',summary:{questoes:4},source:{notion_url:'https://app.notion.com/p/source'},materials:[{...tdas,questoes:undefined,file:'./data/release/materials/tdas.json'},{...outro,questoes:undefined,file:'./data/release/materials/outro.json'},{id:'draft',status:'rascunho',file:'./data/release/materials/draft.json'}]};
assert.deepEqual(selectPublishedMaterials(catalog).map(x=>x.id),['tdas-material','outra-prova']);const first=buildTdasSnapshot(catalog,[tdas,outro],{sourceSha:'a'.repeat(40)}),second=buildTdasSnapshot(catalog,[tdas,outro],{sourceSha:'a'.repeat(40)});assert.deepEqual(first,second,'Snapshot deve ser determinístico.');assert.equal(first.publicSnapshot.schemaVersion,'2.0.0');assert.equal(first.publicSnapshot.scope,'full-published-release');assert.equal(first.publicSnapshot.questionCount,4);assert.equal(first.publicSnapshot.materialCount,2);assert.equal(first.publicSnapshot.targetCargo.questionCount,2);assert.equal(first.publicSnapshot.questionIndex.every(q=>q.lazy===true&&!('gabarito'in q)&&!('alternativas'in q)),true,'Índice público deve ser leve e cego.');assert.equal(first.publicChunks.size,2);assert.equal(first.keyChunks.size,2);const publicPayload=[...first.publicChunks.values()][0],keyPayload=[...first.keyChunks.values()][0];assert.equal(JSON.stringify(publicPayload).includes('"gabarito"'),false);assert.ok(keyPayload.answers[0].comentario);assert.ok(keyPayload.answers[0].fundamento);const ceKey=[...first.keyChunks.values()].flatMap(x=>x.answers).find(x=>x.id==='CE1');assert.equal(ceKey.gabarito,'Certo');assert.throws(()=>buildTdasSnapshot({...catalog,materials:[{...catalog.materials[0],quantidade_questoes:3}]},[tdas],{sourceSha:'b'.repeat(40)}),/Quantidade divergente/);console.log('Banco publicado testado: release integral, índice leve, chunks sob demanda, C/E, múltipla escolha e correção rica separada.');
