import assert from'node:assert/strict';
import{spawn}from'node:child_process';
import{createServer}from'node:http';
import fs from'node:fs/promises';
import path from'node:path';
import os from'node:os';
const ROOT=process.cwd(),PREFIX='/sedes-tdas-dashboard/',port=Number(process.env.TDAS_COMMAND_TEST_PORT||4183),chromePort=Number(process.env.TDAS_COMMAND_CHROME_PORT||9565),chromeBin=process.env.CHROME_BIN||'google-chrome',base=`http://127.0.0.1:${port}${PREFIX.replace(/\/$/,'')}`;
const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.webmanifest':'application/manifest+json','.svg':'image/svg+xml','.png':'image/png'};
const server=createServer(async(req,res)=>{try{const url=new URL(req.url,'http://localhost'),clean=decodeURIComponent(url.pathname);if(!clean.startsWith(PREFIX)){res.writeHead(404);res.end('not found');return}let relative=clean.slice(PREFIX.length);if(!relative||relative.endsWith('/'))relative+='index.html';const file=path.resolve(ROOT,relative);if(!file.startsWith(ROOT)){res.writeHead(403);res.end('forbidden');return}const body=await fs.readFile(file);res.writeHead(200,{'content-type':mime[path.extname(file)]||'application/octet-stream','cache-control':'no-store'});res.end(body)}catch{res.writeHead(404);res.end('not found')}});
await new Promise((resolve,reject)=>{server.once('error',reject);server.listen(port,'127.0.0.1',resolve)});
const profile=await fs.mkdtemp(path.join(os.tmpdir(),'tdas-command-browser-')),chrome=spawn(chromeBin,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage','--remote-debugging-address=127.0.0.1',`--remote-debugging-port=${chromePort}`,`--user-data-dir=${profile}`,'about:blank'],{stdio:['ignore','ignore','pipe']});let chromeError='';chrome.stderr.on('data',chunk=>chromeError+=chunk);const delay=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function waitJson(url,attempts=160){let last;for(let i=0;i<attempts;i++){try{const response=await fetch(url);if(response.ok)return response.json();last=new Error(String(response.status))}catch(error){last=error}await delay(200)}throw new Error(`${last?.message||'timeout'}${chromeError?`\n${chromeError.slice(-1000)}`:''}`)}
function connect(wsUrl){const socket=new WebSocket(wsUrl);let id=0;const pending=new Map(),listeners=new Map();socket.onmessage=event=>{const message=JSON.parse(event.data);if(message.id){const item=pending.get(message.id);if(!item)return;pending.delete(message.id);message.error?item.reject(new Error(message.error.message)):item.resolve(message.result);return}for(const fn of listeners.get(message.method)||[])fn(message.params);listeners.delete(message.method)};const ready=new Promise((resolve,reject)=>{socket.onopen=resolve;socket.onerror=()=>reject(new Error('Falha no DevTools'))});const send=async(method,params={})=>{await ready;const current=++id;return new Promise((resolve,reject)=>{pending.set(current,{resolve,reject});socket.send(JSON.stringify({id:current,method,params}))})};const once=method=>new Promise(resolve=>{const list=listeners.get(method)||[];list.push(resolve);listeners.set(method,list)});return{socket,ready,send,once}}
async function page(width=1100,height=900){const target=await fetch(`http://127.0.0.1:${chromePort}/json/new?about:blank`,{method:'PUT'}).then(r=>r.json()),client=connect(target.webSocketDebuggerUrl);await client.ready;await client.send('Page.enable');await client.send('Runtime.enable');await client.send('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:false});return client}
async function nav(client,url){const loaded=client.once('Page.loadEventFired');await client.send('Page.navigate',{url});await loaded}
async function evalJs(client,expression){const result=await client.send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.text||'Erro no navegador');return result.result?.value}
async function waitFor(client,expression,label,attempts=140){for(let i=0;i<attempts;i++){try{if(await evalJs(client,expression))return}catch{}await delay(120)}throw new Error(`Timeout: ${label}`)}
async function stop(){server.close();if(chrome.exitCode!==null)return;await new Promise(resolve=>{const timer=setTimeout(()=>{if(chrome.exitCode===null)chrome.kill('SIGKILL');resolve()},2200);chrome.once('exit',()=>{clearTimeout(timer);resolve()});chrome.kill('SIGTERM')})}
try{
 await waitJson(`http://127.0.0.1:${chromePort}/json/version`);const catalog=await waitJson(`${base}/data/integration/question-catalog.json`,40),hasDailyQuestions=Array.isArray(catalog.questions)&&catalog.questions.length>0;
 const home=await page();await nav(home,`${base}/`);await waitFor(home,"document.querySelector('[data-command-open]')&&document.querySelector('[data-command-overlay]')",'Command Palette carregada na Home');
 await evalJs(home,"document.querySelector('[data-command-open]').click();true");await waitFor(home,"!document.querySelector('[data-command-overlay]').hidden",'Palette aberta pelo botão');
 assert.equal(await evalJs(home,"document.activeElement===document.querySelector('[data-command-input]')"),true,'Busca da palette deve receber foco ao abrir.');
 await evalJs(home,"(()=>{const input=document.querySelector('[data-command-input]');input.value='PE88';input.dispatchEvent(new Event('input',{bubbles:true}));return true})()");await waitFor(home,"document.querySelector('[data-command-results]')?.textContent.includes('PE88')",'PE88 encontrado');
 assert.equal(await evalJs(home,"document.querySelector('[data-command-results]')?.textContent.includes('Abrir execução diária')"),true,'PE deve apontar para execução diária.');
 await evalJs(home,"document.dispatchEvent(new KeyboardEvent('keydown',{key:'Escape',bubbles:true}));true");await waitFor(home,"document.querySelector('[data-command-overlay]').hidden",'Palette fechada por Escape');
 await evalJs(home,"document.dispatchEvent(new KeyboardEvent('keydown',{key:'k',ctrlKey:true,bubbles:true}));true");await waitFor(home,"!document.querySelector('[data-command-overlay]').hidden",'Palette aberta por Ctrl+K');
 await evalJs(home,"document.dispatchEvent(new KeyboardEvent('keydown',{key:'Escape',bubbles:true}));true");

 const resolver=await page();await nav(resolver,`${base}/resolver/`);await waitFor(resolver,hasDailyQuestions?"document.querySelector('[data-command-open]')&&document.body.textContent.includes('Correção somente ao finalizar')":`document.querySelector('[data-command-open]')&&document.documentElement.dataset.dailyQuestionContext===${JSON.stringify(String(catalog.peId||''))}`,'Palette e estado do Resolver');
 await evalJs(resolver,"document.dispatchEvent(new KeyboardEvent('keydown',{key:'k',ctrlKey:true,bubbles:true}));true");await waitFor(resolver,"!document.querySelector('[data-command-overlay]').hidden",'Palette global no Resolver');
 assert.equal(await evalJs(resolver,"document.querySelector('[data-command-results]')?.textContent.includes('Resolver questões')"),true,'Palette deve conter ações operacionais.');
 await evalJs(resolver,"document.dispatchEvent(new KeyboardEvent('keydown',{key:'Escape',bubbles:true}));true");
 const canStart=await evalJs(resolver,"Boolean(document.querySelector('[data-module-start],[data-module-resume]'))");if(canStart){await evalJs(resolver,"document.querySelector('[data-module-start],[data-module-resume]').click();true");await waitFor(resolver,"document.documentElement.classList.contains('tdas-player-focus')&&document.querySelector('.pilot-question')",'player focado');assert.equal(await evalJs(resolver,"document.querySelector('[data-command-overlay]').hidden"),true,'Palette deve estar fechada ao iniciar questão.');await evalJs(resolver,"document.dispatchEvent(new KeyboardEvent('keydown',{key:'k',ctrlKey:true,bubbles:true}));true");await delay(180);assert.equal(await evalJs(resolver,"document.querySelector('[data-command-overlay]').hidden"),true,'Ctrl+K não pode interromper uma questão ativa.');}else{assert.equal(hasDailyQuestions,false,'Resolver com questões deveria oferecer início ou retomada da sessão.');assert.equal(await evalJs(resolver,"Boolean(document.querySelector('.pilot-question'))"),false,'Dia sem questões não pode fabricar player ativo.');}
 console.log(`Browser Command Palette aprovado: busca PE, teclado global e ${hasDailyQuestions?'bloqueio durante questão':'estado diário sem questões'}.`);
}finally{await stop();await fs.rm(profile,{recursive:true,force:true}).catch(()=>{})}