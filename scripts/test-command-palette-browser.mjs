import assert from'node:assert/strict';import{spawn}from'node:child_process';import{createServer}from'node:http';import fs from'node:fs/promises';import path from'node:path';import os from'node:os';
const ROOT=process.cwd(),PREFIX='/sedes-tdas-dashboard/',port=Number(process.env.TDAS_COMMAND_TEST_PORT||4183),chromePort=Number(process.env.TDAS_COMMAND_CHROME_PORT||9565),chromeBin=process.env.CHROME_BIN||'google-chrome',base=`http://127.0.0.1:${port}${PREFIX.replace(/\/$/,'')}`;const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.webmanifest':'application/manifest+json','.svg':'image/svg+xml','.png':'image/png'};
const server=createServer(async(req,res)=>{try{const url=new URL(req.url,'http://localhost'),clean=decodeURIComponent(url.pathname);if(!clean.startsWith(PREFIX)){res.writeHead(404);res.end('not found');return}let relative=clean.slice(PREFIX.length);if(!relative||relative.endsWith('/'))relative+='index.html';const file=path.resolve(ROOT,relative);if(!file.startsWith(ROOT)){res.writeHead(403);res.end('forbidden');return}const body=await fs.readFile(file);res.writeHead(200,{'content-type':mime[path.extname(file)]||'application/octet-stream','cache-control':'no-store'});res.end(body)}catch{res.writeHead(404);res.end('not found')}});await new Promise((resolve,reject)=>{server.once('error',reject);server.listen(port,'127.0.0.1',resolve)});
const profile=await fs.mkdtemp(path.join(os.tmpdir(),'tdas-command-browser-')),chrome=spawn(chromeBin,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage','--remote-debugging-address=127.0.0.1',`--remote-debugging-port=${chromePort}`,`--user-data-dir=${profile}`,'about:blank'],{stdio:['ignore','ignore','pipe']});let chromeError='';chrome.stderr.on('data',chunk=>chromeError+=chunk);const delay=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function waitJson(url,attempts=160){let last;for(let i=0;i<attempts;i++){try{const r=await fetch(url);if(r.ok)return r.json();last=new Error(String(r.status))}catch(e){last=e}await delay(200)}throw new Error(`${last?.message||'timeout'}${chromeError?`\n${chromeError.slice(-1000)}`:''}`)}function connect(wsUrl){const socket=new WebSocket(wsUrl);let id=0;const pending=new Map(),listeners=new Map();socket.onmessage=e=>{const m=JSON.parse(e.data);if(m.id){const p=pending.get(m.id);if(!p)return;pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result);return}for(const fn of listeners.get(m.method)||[])fn(m.params);listeners.delete(m.method)};const ready=new Promise((resolve,reject)=>{socket.onopen=resolve;socket.onerror=()=>reject(new Error('Falha no DevTools'))});const send=async(method,params={})=>{await ready;const current=++id;return new Promise((resolve,reject)=>{pending.set(current,{resolve,reject});socket.send(JSON.stringify({id:current,method,params}))})};const once=method=>new Promise(resolve=>{const list=listeners.get(method)||[];list.push(resolve);listeners.set(method,list)});return{ready,send,once}}
async function page(width=1100,height=900){const target=await fetch(`http://127.0.0.1:${chromePort}/json/new?about:blank`,{method:'PUT'}).then(r=>r.json()),client=connect(target.webSocketDebuggerUrl);await client.ready;await client.send('Page.enable');await client.send('Runtime.enable');await client.send('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:false});return client}async function nav(client,url){const loaded=client.once('Page.loadEventFired');await client.send('Page.navigate',{url});await loaded}async function evalJs(client,expression){const r=await client.send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.text||'Erro');return r.result?.value}async function waitFor(client,expression,label,attempts=140){for(let i=0;i<attempts;i++){try{if(await evalJs(client,expression))return}catch{}await delay(120)}throw new Error(`Timeout: ${label}`)}async function stop(){server.close();if(chrome.exitCode!==null)return;await new Promise(resolve=>{const t=setTimeout(()=>{if(chrome.exitCode===null)chrome.kill('SIGKILL');resolve()},2200);chrome.once('exit',()=>{clearTimeout(t);resolve()});chrome.kill('SIGTERM')})}
try{await waitJson(`http://127.0.0.1:${chromePort}/json/version`);const home=await page();await nav(home,`${base}/`);await waitFor(home,"document.querySelector('[data-site-search]')&&document.querySelector('[data-command-overlay]')",'Palette carregada');assert.equal(await evalJs(home,"document.querySelectorAll('[data-site-search],[data-command-open]').length"),1);await evalJs(home,"document.querySelector('[data-site-search]').click();true");await waitFor(home,"!document.querySelector('[data-command-overlay]').hidden",'Palette aberta');assert.equal(await evalJs(home,"document.querySelectorAll('[data-command-results] a').length"),3,'Sem busca, a palette deve mostrar só três destinos principais.');assert.equal(await evalJs(home,"document.querySelector('[data-command-results]')?.textContent.includes('Pós-prova')&&document.querySelector('[data-command-results]')?.textContent.includes('Histórico')&&document.querySelector('[data-command-results]')?.textContent.includes('Arquivo do ciclo')"),true);await evalJs(home,"(()=>{const input=document.querySelector('[data-command-input]');input.value='PE88';input.dispatchEvent(new Event('input',{bubbles:true}));return true})()");await waitFor(home,"document.querySelector('[data-command-results]')?.textContent.includes('PE88')",'PE88 encontrado');assert.equal(await evalJs(home,"[...document.querySelectorAll('[data-command-results] a')].some(a=>a.textContent.includes('PE88')&&new URL(a.href).pathname.endsWith('/estudar/')&&new URL(a.href).searchParams.get('pe')==='PE88')"),true);assert.equal(await evalJs(home,"document.querySelector('[data-command-results]')?.textContent.includes('PE de hoje')"),false);await evalJs(home,"document.dispatchEvent(new KeyboardEvent('keydown',{key:'Escape',bubbles:true}));true");await waitFor(home,"document.querySelector('[data-command-overlay]').hidden",'Palette fechada');console.log('Command Palette v3 aprovada: 3 destinos principais e arquivo pesquisável por PE/matéria/recurso.')}finally{await stop();await fs.rm(profile,{recursive:true,force:true}).catch(()=>{})}
