import{loadData,setupShell,fmtNumber,fmtPct,metric,renderBars,alertCard,escapeHTML,routes,setLoadingError}from'./common.js?v=20260807.3';
try{
 const d=await loadData();setupShell('riscos',d.meta);const errors=d.errors||[],total=Number(d.metrics.errors||errors.length),missing=Math.max(0,total-errors.length),high=errors.filter(x=>['Alta','Crítica'].includes(x.severity)).length,discursive=errors.filter(x=>x.discursive).length;
 const subjects=[...new Set(errors.map(x=>x.subject))].map(subject=>{const rows=errors.filter(x=>x.subject===subject);return{subject,errors:rows.length,high_critical:rows.filter(x=>['Alta','Crítica'].includes(x.severity)).length,discursive:rows.filter(x=>x.discursive).length}}).sort((a,b)=>b.errors-a.errors);
 document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Diagnóstico acionável</span><h1>Riscos e erros</h1><p>Concentração, gravidade e potencial discursivo para decidir o que revisar primeiro.</p><div class="hero-actions"><a class="btn primary" href="${routes.caderno}">Abrir caderno integrado</a><a class="btn" href="${routes.materias}">Ver matérias</a></div></section><section class="grid metrics">${metric('Erros oficiais',fmtNumber(total),'total confirmado no Notion')}${metric('Detalhados',fmtNumber(errors.length),total?fmtPct(errors.length/total*100,1)+' de cobertura':'sem erros')}${metric('A recuperar',fmtNumber(missing),'registros oficiais sem detalhe individual')}${metric('Altos + críticos',fmtNumber(high),'entre os registros detalhados')}</section>${missing?`<section class="section"><article class="card alert" data-level="warning"><span class="alert-icon">△</span><div><b>${missing} erro${missing>1?'s':''} oficial${missing>1?'is':''} ainda sem diagnóstico individual</b><p>O total oficial permanece preservado, mas estes registros não entram ainda no Pareto detalhado. A sincronização deve recuperar cada registro antes de classificá-lo por matéria, tema ou gravidade.</p></div><a href="${routes.auditoria}">Ver qualidade dos dados →</a></article></section>`:''}<section class="section"><div class="section-head"><div><h2>Pareto por matéria</h2><p>Somente sobre os registros individualmente recuperados.</p></div></div><div class="toolbar"><label>Métrica<select id="metric"><option value="errors">Total detalhado</option><option value="high_critical">Altos + críticos</option><option value="discursive">Discursáveis</option></select></label><label>Matéria<select id="subject"><option value="all">Todas</option>${subjects.map(x=>`<option>${escapeHTML(x.subject)}</option>`).join('')}</select></label><label>Busca<input type="search" id="search" placeholder="Tema do erro"></label></div><article class="card panel" id="risk-bars"></article></section><section class="section"><div class="section-head"><div><h2>Alertas inteligentes</h2><p>Cada alerta aponta a próxima ação.</p></div></div><div class="grid two">${(d.alerts||[]).map(alertCard).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Erros detalhados</h2><p>${errors.length} de ${total} registros oficiais disponíveis para diagnóstico.</p></div></div><div class="table-wrap"><table><thead><tr><th>Origem</th><th>Matéria</th><th>Tema</th><th>Gravidade</th><th>Discursável</th></tr></thead><tbody id="error-rows"></tbody></table></div></section><footer class="footer"><span>Riscos · ${errors.length}/${total} erros detalhados</span><span>Snapshot <span data-snapshot></span></span></footer>`;
 const metricSel=document.querySelector('#metric'),subjectSel=document.querySelector('#subject'),search=document.querySelector('#search'),bars=document.querySelector('#risk-bars'),tbody=document.querySelector('#error-rows');function update(){const term=search.value.trim().toLowerCase();const filtered=errors.filter(x=>(subjectSel.value==='all'||x.subject===subjectSel.value)&&(!term||`${x.topic} ${x.subject} ${x.sprint}`.toLowerCase().includes(term))),grouped=[...new Set(filtered.map(x=>x.subject))].map(subject=>{const rows=filtered.filter(x=>x.subject===subject);return{subject,errors:rows.length,high_critical:rows.filter(x=>['Alta','Crítica'].includes(x.severity)).length,discursive:rows.filter(x=>x.discursive).length}});renderBars(bars,grouped,{labelKey:'subject',valueKey:metricSel.value});tbody.innerHTML=filtered.map(item=>`<tr><td>${escapeHTML(item.sprint)}</td><td>${escapeHTML(item.subject)}</td><td>${escapeHTML(item.topic)}</td><td><span class="status ${item.severity==='Alta'||item.severity==='Crítica'?'critical':'warning'}">${escapeHTML(item.severity)}</span></td><td>${item.discursive?'Sim':'Não'}</td></tr>`).join('')||'<tr><td colspan="5">Nenhum erro para o filtro.</td></tr>'}metricSel.onchange=subjectSel.onchange=update;search.oninput=update;update();
}catch(error){setLoadingError(error)}
