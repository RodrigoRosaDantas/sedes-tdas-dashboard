import{loadData,setupShell,fmtNumber,fmtDate,escapeHTML,routes,slug,setLoadingError}from'./common.js?v=20260807.1';
try{
 const d=await loadData();setupShell('materias',d.meta);
 const map=new Map();
 for(const e of d.errors||[]){const key=e.subject||'Sem matéria',row=map.get(key)||{subject:key,errors:0,high:0,discursive:0,latest:null,topics:new Set()};row.errors++;if(/alta|crítica/i.test(e.severity||''))row.high++;if(e.discursive)row.discursive++;if(e.topic)row.topics.add(e.topic);if(!row.latest||String(e.date)>row.latest)row.latest=e.date;map.set(key,row)}
 const rows=[...map.values()].sort((a,b)=>b.errors-a.errors);
 document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Diagnóstico consolidado</span><h1>Matérias</h1><p>Abra cada matéria para consultar erros recuperados, Sprints relacionados, criticidade e potencial discursivo.</p><div class="hero-actions"><a class="btn" href="${routes.caderno}">Abrir caderno de erros</a><a class="btn" href="${routes.riscos}">Abrir riscos</a></div></section><section class="section"><div class="grid portal-grid">${rows.map(x=>`<a class="card subject-card" href="${routes.materia}?id=${encodeURIComponent(x.subject)}"><small>${fmtNumber(x.errors)} erros detalhados · último ${fmtDate(x.latest)}</small><b>${escapeHTML(x.subject)}</b><span>${x.high} altos/críticos · ${x.discursive} com potencial discursivo · ${x.topics.size} temas</span><em>Abrir diagnóstico →</em></a>`).join('')||'<div class="empty">Nenhuma matéria detalhada no snapshot atual.</div>'}</div></section><section class="section"><article class="card panel"><h2>Cobertura</h2><p>Esta página usa somente os registros individuais recuperados com segurança. O total oficial do Caderno de Erros permanece preservado mesmo quando alguns registros ainda não estão detalhados.</p></article></section><footer class="footer"><span>${rows.length} matérias detalhadas</span><span>Snapshot <span data-snapshot></span></span></footer>`;
}catch(error){setLoadingError(error)}
