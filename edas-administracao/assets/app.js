import{RELEASE,loadData,setupShell,fmtNumber,fmtPct,fmtDate,metric,alertCard,routes,countdown,planDiagnostics,recoveryAdvice,setupTodaySession,renderLineChart,setLoadingError,escapeHTML}from'./common.js?v=20260801.3';
const page=document.body.dataset.page||'home';
const params=new URLSearchParams(location.search);
try{
 const d=await loadData();window.__EDAS_DATA__=d;setupShell(page,d.meta);
 const main=document.querySelector('main'),m=d.metrics,diag=planDiagnostics(d),recovery=recoveryAdvice(diag,d),days=countdown(d.meta.examDate);
 const progress=m.completed/m.total*100;
 const hero=(kicker,title,text,actions='')=>`<section class="hero"><span class="kicker">${kicker}</span><h1>${title}</h1><p>${text}</p>${actions}</section>`;
 const footer=`<footer class="footer"><span>EDAS — Administração · Cargo 400 · v<span data-version>${RELEASE}</span></span><span>Sincronização: <span data-sync></span></span></footer>`;
 const bars=(rows,valueKey='value',suffix='%')=>{const values=rows.map(x=>Number(x[valueKey]||0)),max=Math.max(1,...values);return`<div class="bars">${rows.map(x=>`<div class="bar-row"><span>${escapeHTML(x.label)}</span><div class="bar-track"><div class="bar-fill" style="width:${Math.max(2,Number(x[valueKey]||0)/max*100)}%"></div></div><strong class="bar-value">${suffix==='%'?fmtPct(x[valueKey]):fmtNumber(x[valueKey])}</strong></div>`).join('')}</div>`};
 const metricGrid=`<section class="grid metrics">${metric('Ciclo concluído',`${m.completed}/${m.total}`,`${progress.toFixed(1).replace('.',',')}% do plano`)}${metric('Questões concluídas',fmtNumber(m.questions),'execução real registrada')}${metric('Acertos registrados',fmtNumber(m.correct),`${fmtNumber(m.questions)} questões com resultado`)}${metric('Aproveitamento',fmtPct(m.accuracy),'bloco objetivo executado')}</section>`;
 if(page==='home'){
  const expectedPct=diag.expected/diag.total*100,gapText=(diag.gap>=0?'+':'')+diag.gap.toFixed(1).replace('.',',');
  main.innerHTML=`${hero('Plataforma de preparação','EDAS — Administração, Cargo 400 | SEDES/DF','Cada Sprint concluído transforma preparação em desempenho. Acompanhe sua evolução, corrija os pontos frágeis e mantenha o ritmo até a prova — com foco, constância e dados reais.',`<div class="tags"><span class="tag">⏳ Faltam ${days} dias para a prova</span><span class="tag">Prova: ${fmtDate(d.meta.examDate)}</span><span class="tag">${diag.remaining} Sprints restantes</span></div><div class="hero-actions"><a class="btn primary" href="${routes.hoje}">Abrir foco de hoje</a><a class="btn" href="${routes.evolucao}">Ver evolução</a></div>`)}${metricGrid}<section class="section"><div class="section-head"><div><h2>Progresso geral</h2><p>Realizado, esperado pela data e diferença de ritmo.</p></div><span class="status-pill ${diag.level}">${diag.label}</span></div><div class="progress-compare"><article class="card progress-card"><small>Progresso realizado</small><strong>${progress.toFixed(1).replace('.',',')}%</strong><span>${m.completed} de ${m.total} Sprints</span><div class="progress-track"><div class="progress-fill" style="width:${Math.min(100,progress)}%"></div></div></article><article class="card progress-card"><small>Progresso esperado</small><strong>${expectedPct.toFixed(1).replace('.',',')}%</strong><span>${diag.expected.toFixed(1).replace('.',',')} Sprints pelo ritmo linear</span><div class="progress-track"><div class="progress-fill expected" style="width:${Math.min(100,expectedPct)}%"></div></div></article><article class="card progress-card"><small>Diferença de ritmo</small><strong>${gapText}</strong><span>Ritmo mínimo: ${diag.pace.toFixed(2).replace('.',',')} Sprint/dia<br>Conclusão projetada: ${diag.projectedDate?fmtDate(diag.projectedDate):'sem base suficiente'}</span></article></div><article class="card recovery-plan" data-level="${recovery.level}"><span class="kicker">${recovery.title}</span><h3>${escapeHTML(recovery.detail)}</h3><p>O cálculo não substitui o cronograma oficial: ele serve para orientar a recuperação sem eliminar descanso, revisão ou correção ativa.</p></article></section><section class="section"><div class="section-head"><div><h2>Hoje</h2><p>Uma tela operacional para executar o Sprint sem dispersão.</p></div><span class="stamp">${d.today.sprint} · ${d.today.status}</span></div><div class="grid two"><a class="card focus-main" href="${routes.hoje}"><span class="kicker">Sprint prioritário</span><h2>${d.today.sprint} — ${d.today.title}</h2><div class="tags"><span class="tag">até ${d.today.planned} questões</span><span class="tag">${d.today.time} min</span><span class="tag">${d.today.review}</span></div><span class="btn primary">Abrir sessão do dia →</span></a><article class="card target"><div><small>Tempo até a prova</small><strong>${days}</strong><span>dias restantes<br>${fmtDate(d.meta.examDate)}</span></div></article></div></section><section class="section"><div class="section-head"><div><h2>Áreas da plataforma</h2><p>Execução, diagnóstico, agenda e governança em módulos independentes.</p></div></div><div class="grid portal-grid"><a class="card portal" href="${routes.evolucao}"><small>Desempenho</small><b>Evolução</b><span>Histórico, blocos e gráfico de aproveitamento.</span><em>Abrir →</em></a><a class="card portal" href="${routes.riscos}"><small>Diagnóstico</small><b>Riscos e erros</b><span>Filtros por matéria, gravidade e Sprint.</span><em>Abrir →</em></a><a class="card portal" href="${routes.agenda}"><small>Execução</small><b>Agenda</b><span>S01–S42, atraso e ritmo necessário.</span><em>Abrir →</em></a><a class="card portal" href="${routes.casos}"><small>Discursiva</small><b>Estudos de caso</b><span>SEC01–SEC12, prontidão e detalhes.</span><em>Abrir →</em></a><a class="card portal" href="${routes.auditoria}"><small>Governança</small><b>Auditoria</b><span>Fontes, qualidade e downloads reais.</span><em>Abrir →</em></a></div></section><section class="section"><div class="section-head"><div><h2>Alertas prioritários</h2><p>Problema, impacto e próximo passo.</p></div></div><div class="grid three">${d.alerts.map(alertCard).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Projeções transparentes</h2><p>Nenhum índice oculto: a fórmula aparece junto ao resultado.</p></div></div><div class="grid three">${d.projections.map(x=>`<article class="card formula"><small>${x.label}</small><strong>${x.value}</strong><code>${x.formula}</code></article>`).join('')}</div></section>${footer}`
 }
 if(page==='hoje'){
  main.innerHTML=`${hero('Foco de execução',`${d.today.sprint} — ${d.today.title}`,'Sessão local com checklist, cronômetro e notas. O progresso fica salvo somente neste aparelho e não altera o Notion.',`<div class="tags"><span class="tag">até ${d.today.planned} questões</span><span class="tag">${d.today.time} minutos</span><span class="tag">${d.today.status}</span></div>`)}<section class="section"><div class="session"><article class="card focus-main"><div class="section-head"><div><h2>Ordem de execução</h2><p>Marque cada etapa após a conclusão real.</p></div></div><div class="checklist">${d.today.steps.map(step=>`<label class="card check"><input class="check-box" type="checkbox" data-session-check="${step.id}"><span><b>${escapeHTML(step.label)}</b><small>${escapeHTML(step.detail)}</small></span><time>${step.minutes} min</time></label>`).join('')}</div><div class="session-progress"><b><span>Progresso da sessão</span><span data-session-progress-text>0/${d.today.steps.length} etapas</span></b><div class="progress-track"><div class="progress-fill" data-session-progress style="width:0"></div></div></div></article><div class="grid"><article class="card timer-card"><span class="kicker">Cronômetro do Sprint</span><div class="timer-display" data-timer-display>00:00:00</div><div class="timer-meta">Meta prevista: ${d.today.time} minutos</div><div class="timer-controls"><button class="btn primary" data-timer-start>Iniciar</button><button class="btn" data-timer-pause>Pausar</button><button class="btn danger" data-timer-reset>Zerar</button></div></article><article class="card notes-card"><h3>Dúvidas, chutes e observações</h3><textarea data-session-notes placeholder="Registre somente o essencial para a correção ativa."></textarea><div class="saved" data-saved></div><div class="quick-actions"><button class="btn" data-copy-summary>Copiar resumo</button><button class="btn danger" data-session-reset>Limpar sessão</button></div></article></div></div></section><section class="section"><div class="section-head"><div><h2>Acessos rápidos</h2><p>Abra as fontes oficiais sem procurar manualmente.</p></div></div><div class="quick-actions"><a class="btn primary" href="${d.today.links.sprint}" target="_blank" rel="noopener">Abrir S06 no Notion ↗</a><a class="btn" href="${d.today.links.control}" target="_blank" rel="noopener">Controle de questões ↗</a><a class="btn" href="${d.today.links.errors}" target="_blank" rel="noopener">Caderno de erros ↗</a><a class="btn" href="${d.today.links.cases}" target="_blank" rel="noopener">Estudos de caso ↗</a></div></section><section class="section"><div class="section-head"><div><h2>Regra de decisão</h2><p>Protocolo adaptativo após a correção.</p></div></div><div class="grid three"><article class="card panel"><h3>≥ 80%</h3><p>Seguir o ciclo e revisar somente dúvidas e chutes relevantes.</p></article><article class="card panel"><h3>60% a 79%</h3><p>Criar revisão de 24 horas dos pontos frágeis.</p></article><article class="card panel"><h3>&lt; 60%</h3><p>Reestudar o núcleo errado e resolver bateria curta antes de avançar.</p></article></div></section>${footer}`;setupTodaySession(d.today.sprint,d.today.steps)
 }
 if(page==='evolucao'){
  main.innerHTML=`${hero('Desempenho','Evolução do EDAS','Acompanhe resultados reais por Sprint e por bloco, mantendo planejamento e execução em camadas separadas.')}${metricGrid}<section class="section"><div class="section-head"><div><h2>Curva de aproveitamento</h2><p>Somente Sprints com resultado real.</p></div><span class="stamp">${d.evolution.length} resultados</span></div><article class="card chart" id="evolution-chart"></article></section><section class="section"><div class="section-head"><div><h2>Desempenho por bloco</h2><p>Acumulado dos Sprints executados.</p></div></div><article class="card panel">${bars(d.performance)}</article></section><section class="section"><div class="section-head"><div><h2>Histórico executado</h2><p>Questões, acertos e aproveitamento registrados.</p></div></div><div class="table-wrap"><table><thead><tr><th>Sprint</th><th>Data</th><th>Questões</th><th>Acertos</th><th>Aproveitamento</th></tr></thead><tbody>${d.evolution.map(x=>`<tr><td>${x.sprint}</td><td>${fmtDate(x.date)}</td><td>${x.questions}</td><td>${x.correct}</td><td>${fmtPct(x.accuracy)}</td></tr>`).join('')}</tbody></table></div></section>${footer}`;renderLineChart(document.querySelector('#evolution-chart'),d.evolution)
 }
 if(page==='riscos'){
  const subjects=[...new Set(d.errors.map(x=>x.subject))].sort();
  main.innerHTML=`${hero('Diagnóstico','Riscos e erros','Filtre os registros detalhados disponíveis e transforme cada falha em revisão, regra de prevenção e nova tentativa.')}<section class="grid metrics">${metric('Erros oficiais',m.errors,'total preservado no snapshot')}${metric('Detalhados',d.errorCoverage.loaded,`de ${d.errorCoverage.total} registros`)}${metric('Maior gargalo','Português','66,67% no acumulado')}${metric('Alta gravidade',d.errors.filter(x=>x.severity==='Alta').length,'registros detalhados')}</section><section class="section"><div class="section-head"><div><h2>Distribuição por bloco</h2><p>Total oficial de erros.</p></div></div><article class="card panel">${bars(d.errorsByBlock,'errors','n')}</article></section><section class="section"><div class="section-head"><div><h2>Explorar registros</h2><p>Matéria, gravidade, Sprint e potencial discursivo.</p></div></div><div class="coverage">${escapeHTML(d.errorCoverage.note)} Cobertura detalhada: ${d.errorCoverage.loaded}/${d.errorCoverage.total}.</div><div class="toolbar"><label>Matéria <select id="risk-subject"><option value="">Todas</option>${subjects.map(x=>`<option>${escapeHTML(x)}</option>`).join('')}</select></label><label>Gravidade <select id="risk-severity"><option value="">Todas</option><option>Alta</option><option>Média</option></select></label><label>Sprint <select id="risk-sprint"><option value="">Todos</option>${[...new Set(d.errors.map(x=>x.sprint))].sort().map(x=>`<option>${x}</option>`).join('')}</select></label><input id="risk-search" type="search" placeholder="Buscar tema"></div><div class="table-wrap"><table><thead><tr><th>Data</th><th>Sprint</th><th>Matéria</th><th>Tema</th><th>Gravidade</th><th>Discursável</th></tr></thead><tbody id="risk-body"></tbody></table></div></section><section class="section"><div class="section-head"><div><h2>Prioridades estratégicas</h2></div></div><div class="grid three">${d.alerts.map(alertCard).join('')}</div></section>${footer}`;
  const renderErrors=()=>{const sub=document.querySelector('#risk-subject').value,sev=document.querySelector('#risk-severity').value,spr=document.querySelector('#risk-sprint').value,q=document.querySelector('#risk-search').value.toLowerCase(),rows=d.errors.filter(x=>(!sub||x.subject===sub)&&(!sev||x.severity===sev)&&(!spr||x.sprint===spr)&&(!q||x.topic.toLowerCase().includes(q)));document.querySelector('#risk-body').innerHTML=rows.length?rows.map(x=>`<tr><td>${fmtDate(x.date)}</td><td>${x.sprint}</td><td>${escapeHTML(x.subject)}</td><td>${escapeHTML(x.topic)}</td><td><span class="status ${x.severity==='Alta'?'critical':'warning'}">${x.severity}</span></td><td>${x.discursive?'Sim':'Não'}</td></tr>`).join(''):'<tr><td colspan="6" class="empty">Nenhum registro para os filtros.</td></tr>'};['risk-subject','risk-severity','risk-sprint'].forEach(id=>document.querySelector('#'+id).addEventListener('change',renderErrors));document.querySelector('#risk-search').addEventListener('input',renderErrors);renderErrors()
 }
 if(page==='agenda')main.innerHTML=`${hero('Planejamento','Agenda S01–S42',`Há ${diag.remaining} Sprints restantes e ${days} dias até a prova. O ritmo mínimo atual é de ${diag.pace.toFixed(2).replace('.',',')} Sprint por dia.`)}<section class="grid metrics">${metric('Concluídos',m.completed,`de ${m.total} Sprints`)}${metric('Esperado pela data',diag.expected.toFixed(1).replace('.',','),'projeção linear')}${metric('Dias até a prova',days,fmtDate(d.meta.examDate))}${metric('Ritmo necessário',diag.pace.toFixed(2).replace('.',','),'Sprint por dia')}</section><section class="section"><article class="card recovery-plan" data-level="${recovery.level}"><span class="kicker">${recovery.title}</span><h3>${escapeHTML(recovery.detail)}</h3></article></section><section class="section"><div class="section-head"><div><h2>Ciclo completo</h2><p>Status real quando disponível; demais linhas representam o calendário oficial do ciclo.</p></div></div><div class="table-wrap"><table><thead><tr><th>Sprint</th><th>Data</th><th>Tema</th><th>Status</th><th>Questões</th><th>Aproveitamento</th></tr></thead><tbody>${d.sprints.map(x=>`<tr><td>${x.id}</td><td>${fmtDate(x.date)}</td><td>${escapeHTML(x.title||'Conteúdo mantido no Banco Mestre')}</td><td><span class="status ${x.status==='Atrasado'?'critical':x.status==='Pendente'||x.status==='Em andamento'?'warning':''}">${escapeHTML(x.status)}</span></td><td>${fmtNumber(x.questions)}</td><td>${fmtPct(x.accuracy)}</td></tr>`).join('')}</tbody></table></div></section>${footer}`;
 if(page==='casos'){
  const selected=params.get('case');
  if(selected){const c=d.cases.find(x=>x.id===selected);if(!c)throw new Error('Estudo de caso não encontrado');main.innerHTML=`${hero('Estudo de caso',`${c.id} — ${c.theme}`,'Detalhe operacional, critérios e próxima ação. O conteúdo oficial completo permanece no Notion.',`<div class="hero-actions"><a class="btn primary" href="${c.url}" target="_blank" rel="noopener">Abrir no Notion ↗</a><a class="btn" href="${routes.casos}">Voltar aos casos</a></div>`)}<section class="section"><div class="grid two"><article class="card case-detail"><span class="kicker">Execução</span><h2>${c.status}</h2><div class="tags"><span class="tag">Programado: ${fmtDate(c.date)}</span><span class="tag">D+1: ${fmtDate(c.d1Date)}</span><span class="tag">${c.readiness||'Prontidão não consolidada'}</span></div><p>${escapeHTML(c.next)}</p><h3>Checklist do caso</h3><div class="checklist">${[['allCommands','Todos os comandos respondidos'],['manuscript','Manuscrito concluído'],['diagnosis','Diagnóstico registrado'],['rewrite','Reescrita concluída']].map(([key,label])=>`<article class="card check"><span class="check-box" aria-hidden="true"></span><span><b>${label}</b><small>${c.checks?.[key]?'Registrado':'Pendente'}</small></span></article>`).join('')}</div></article><article class="card case-detail"><span class="kicker">Correção Quadrix</span><div class="score-grid"><div class="score"><small>CAC</small><strong>${fmtNumber(c.scores?.cac)}</strong></div><div class="score"><small>OT</small><strong>${fmtNumber(c.scores?.ot)}</strong></div><div class="score"><small>DLP</small><strong>${fmtNumber(c.scores?.dlp)}</strong></div><div class="score"><small>Nota</small><strong>${fmtNumber(c.scores?.estimated)}</strong></div></div><p>Os campos permanecem vazios enquanto não houver execução e correção real no banco.</p></article></div></section>${footer}`}else main.innerHTML=`${hero('Prova discursiva','Estudos de caso EDAS','Acompanhe SEC01–SEC12, prontidão, execução, correção e D+1 sem confundir planejamento com resultado real.')}<section class="grid metrics">${metric('Casos planejados',m.casesTotal,'SEC01–SEC12')}${metric('Executados',m.casesCompleted,'nenhum texto registrado')}${metric('Prontos e auditados',d.cases.filter(x=>x.readiness==='Pronto — auditado').length,'conforme snapshot')}${metric('Próxima ação','SEC01','executar e realizar D+1')}</section><section class="section"><div class="section-head"><div><h2>Casos programados</h2><p>Abra cada caso para ver a situação e o próximo passo.</p></div></div><div class="grid case-grid">${d.cases.map(c=>`<article class="card case-card"><span class="kicker">${c.id} · ${fmtDate(c.date)}</span><h3>${escapeHTML(c.theme)}</h3><div class="tags"><span class="tag">${c.status}</span><span class="tag">${c.readiness||'Prontidão não consolidada'}</span></div><p>${escapeHTML(c.next)}</p><a class="btn" href="${routes.casos}?case=${c.id}">Abrir detalhe →</a></article>`).join('')}</div></section>${footer}`
 }
 if(page==='auditoria')main.innerHTML=`${hero('Governança','Auditoria, fontes e downloads','Verifique limitações do snapshot, abra as fontes oficiais e exporte as estruturas reais usadas pelo aplicativo.')}<section class="section"><div class="section-head"><div><h2>Qualidade dos dados</h2><p>Pendências, cobertura e divergências sem ocultação.</p></div></div><div class="grid three">${d.quality.map(x=>`<article class="card alert" data-level="${x.level}"><span class="alert-icon">${x.level==='critical'?'!':x.level==='warning'?'△':'i'}</span><div><b>${x.title}</b><p>${x.detail}</p></div></article>`).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Fontes oficiais</h2><p>Bancos independentes do TDAS.</p></div></div><div class="grid three">${d.sources.map(x=>`<a class="card source" href="${x.url}" target="_blank" rel="noopener"><small>Notion</small><b>${x.name}</b><span>${x.detail}</span></a>`).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Downloads reais</h2><p>Gerados a partir do site.json atual, sem linhas fixas no JavaScript.</p></div></div><div class="grid two">${[['sprints','CSV','Sprints S01–S42'],['errors','CSV','Erros detalhados'],['cases','CSV','SEC01–SEC12'],['quality','CSV','Qualidade dos dados'],['snapshot','JSON','Snapshot integral']].map(x=>`<button class="card download" data-download="${x[0]}"><span class="file">${x[1]}</span><span><b>${x[2]}</b><small>Baixar arquivo do snapshot ${fmtDate(d.meta.snapshotDate)}</small></span><span>↓</span></button>`).join('')}</div></section><section class="section"><article class="card panel"><h3>Versão e cache</h3><p>Aplicativo: ${RELEASE} · Snapshot: ${fmtDate(d.meta.snapshotDate)} · modo offline: ${d.meta.offlineFallback?'ativo com último snapshot':'dados atuais carregados'}.</p></article></section>${footer}`;
 if(page==='mais')main.innerHTML=`${hero('Navegação','Mais áreas','Acesse agenda, estudos de caso, auditoria e instalação pelo celular.')}<section class="section"><div class="grid portal-grid"><a class="card portal" href="${routes.agenda}"><small>Execução</small><b>Agenda</b><span>S01–S42 e recuperação de ritmo.</span><em>Abrir →</em></a><a class="card portal" href="${routes.casos}"><small>Discursiva</small><b>Estudos de caso</b><span>SEC01–SEC12 e detalhes.</span><em>Abrir →</em></a><a class="card portal" href="${routes.auditoria}"><small>Governança</small><b>Auditoria</b><span>Fontes, qualidade e downloads.</span><em>Abrir →</em></a></div></section>${footer}`;
 document.querySelectorAll('[data-sync]').forEach(el=>el.textContent=d.meta.syncTimes.join(' · '));
}catch(error){setLoadingError(error)}