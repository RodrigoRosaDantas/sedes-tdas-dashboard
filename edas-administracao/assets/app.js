import{loadData,setupShell,fmtNumber,fmtPct,fmtDate,metric,alertCard,routes,countdown,setLoadingError,escapeHTML}from'./common.js?v=1';
const page=document.body.dataset.page||'home';
try{
 const d=await loadData();window.__EDAS_DATA__=d;setupShell(page,d.meta);
 const main=document.querySelector('main');
 const days=countdown(d.meta.examDate),m=d.metrics,progress=m.completed/m.total*100,remaining=m.total-m.completed,pace=remaining/Math.max(1,days);
 const hero=(kicker,title,text,actions='')=>`<section class="hero"><span class="kicker">${kicker}</span><h1>${title}</h1><p>${text}</p>${actions}</section>`;
 const footer=`<footer class="footer"><span>EDAS — Administração · Cargo 400</span><span>Sincronização: <span data-sync></span></span></footer>`;
 const bars=rows=>`<div class="bars">${rows.map(x=>`<div class="bar-row"><span>${escapeHTML(x.label)}</span><div class="bar-track"><div class="bar-fill" style="width:${Math.max(2,Number(x.value??x.errors)/Math.max(...rows.map(y=>Number(y.value??y.errors)))*100)}%"></div></div><strong class="bar-value">${x.value!==undefined?fmtPct(x.value):fmtNumber(x.errors)}</strong></div>`).join('')}</div>`;
 const metricGrid=`<section class="grid metrics">${metric('Ciclo concluído',`${m.completed}/${m.total}`,`${progress.toFixed(1).replace('.',',')}% do plano`)}${metric('Questões concluídas',fmtNumber(m.questions),'execução real registrada')}${metric('Acertos registrados',fmtNumber(m.correct),`${fmtNumber(m.questions)} questões com resultado`)}${metric('Aproveitamento',fmtPct(m.accuracy),'bloco objetivo executado')}</section>`;
 if(page==='home'){
  main.innerHTML=`
  ${hero('Plataforma de preparação','EDAS — Administração, Cargo 400 | SEDES/DF','Cada Sprint concluído transforma preparação em desempenho. Acompanhe sua evolução, corrija os pontos frágeis e mantenha o ritmo até a prova — com foco, constância e dados reais.',`<div class="tags"><span class="tag">⏳ Faltam ${days} dias para a prova</span><span class="tag">Prova: ${fmtDate(d.meta.examDate)}</span><span class="tag">${remaining} Sprints restantes</span></div><div class="hero-actions"><a class="btn primary" href="${routes.hoje}">Abrir foco de hoje</a><a class="btn" href="${routes.evolucao}">Ver evolução</a></div>`)}
  ${metricGrid}
  <section class="section"><div class="section-head"><div><h2>Hoje</h2><p>Uma única tela para saber exatamente o que executar.</p></div><span class="stamp">${d.today.sprint} · ${d.today.status}</span></div><div class="grid two"><a class="card focus-main" href="${routes.hoje}"><span class="kicker">Sprint prioritário</span><h2>${d.today.sprint} — ${d.today.title}</h2><div class="tags"><span class="tag">${d.today.planned} questões</span><span class="tag">${d.today.block}</span><span class="tag">${d.today.review}</span></div><span class="btn primary">Abrir plano do Sprint →</span></a><article class="card target"><div><small>Tempo até a prova</small><strong>${days}</strong><span>dias restantes<br>${fmtDate(d.meta.examDate)}</span></div></article></div></section>
  <section class="section"><div class="section-head"><div><h2>Áreas da plataforma</h2><p>Os detalhes ficam em páginas independentes e mais leves.</p></div><span class="stamp">Navegação direta</span></div><div class="grid portal-grid"><a class="card portal" href="${routes.evolucao}"><small>Desempenho</small><b>Evolução</b><span>Sprints, blocos e aproveitamento.</span><em>Abrir →</em></a><a class="card portal" href="${routes.riscos}"><small>Diagnóstico</small><b>Riscos e erros</b><span>Pareto, gargalos e próximos passos.</span><em>Abrir →</em></a><a class="card portal" href="${routes.agenda}"><small>Execução</small><b>Agenda</b><span>Próximos Sprints e ritmo necessário.</span><em>Abrir →</em></a><a class="card portal" href="${routes.casos}"><small>Discursiva</small><b>Estudos de caso</b><span>SEC01–SEC12, prontidão e execução.</span><em>Abrir →</em></a><a class="card portal" href="${routes.auditoria}"><small>Governança</small><b>Auditoria</b><span>Qualidade, fontes e downloads.</span><em>Abrir →</em></a></div></section>
  <section class="section"><div class="section-head"><div><h2>Alertas prioritários</h2><p>Problema, impacto e próximo passo.</p></div></div><div class="grid three">${d.alerts.map(alertCard).join('')}</div></section>
  <section class="section"><div class="section-head"><div><h2>Projeções transparentes</h2><p>Nenhum índice oculto: a fórmula aparece junto ao resultado.</p></div></div><div class="grid three">${d.projections.map(x=>`<article class="card formula"><small>${x.label}</small><strong>${x.value}</strong><code>${x.formula}</code></article>`).join('')}</div></section>${footer}`;
 }
 if(page==='hoje'){
  main.innerHTML=`${hero('Foco de execução',`${d.today.sprint} — ${d.today.title}`,`Sprint programado para ${fmtDate(d.today.scheduledDate)} e ainda sem execução real registrada. A prioridade é concluir o bloco antes de acumular novos Sprints.`,`<div class="tags"><span class="tag">${d.today.planned} questões</span><span class="tag">${d.today.time} minutos</span><span class="tag">${d.today.status}</span></div>`)}
  <section class="section"><div class="grid focus"><article class="card focus-main"><span class="kicker">Ordem de execução</span><h2>${d.today.block}</h2><div class="checklist">${[['Revisão de abertura',d.today.review],['Material do dia',d.today.material],['Conhecimentos comuns',d.today.common],['Microdose de Português',d.today.portuguese],['Entregável',d.today.deliverable],['Fechamento','Correção ativa e registro dos resultados reais']].map(x=>`<article class="card check"><span class="check-box"></span><div><b>${x[0]}</b><small>${x[1]}</small></div></article>`).join('')}</div></article><article class="card target"><div><small>Ritmo necessário</small><strong>${pace.toFixed(2).replace('.',',')}</strong><span>Sprint por dia<br>${remaining} restantes em ${days} dias</span></div></article></div></section>
  <section class="section"><div class="section-head"><div><h2>Regra de decisão</h2><p>Protocolo adaptativo após a correção.</p></div></div><div class="grid three"><article class="card panel"><h3>≥ 80%</h3><p>Seguir o ciclo e revisar somente dúvidas e chutes relevantes.</p></article><article class="card panel"><h3>60% a 79%</h3><p>Criar revisão de 24 horas dos pontos frágeis.</p></article><article class="card panel"><h3>&lt; 60%</h3><p>Reestudar o núcleo errado e resolver bateria curta antes de avançar.</p></article></div></section>${footer}`;
 }
 if(page==='evolucao'){
  main.innerHTML=`${hero('Desempenho','Evolução do EDAS','Acompanhe o resultado real por Sprint e por bloco, sem misturar planejamento com execução.')}
  ${metricGrid}
  <section class="section"><div class="section-head"><div><h2>Desempenho por bloco</h2><p>Resultado do S01.</p></div><span class="stamp">27/07/2026</span></div><article class="card panel">${bars(d.performance)}</article></section>
  <section class="section"><div class="section-head"><div><h2>Histórico executado</h2><p>Somente Sprints com resultado real.</p></div></div><div class="table-wrap"><table><thead><tr><th>Sprint</th><th>Data</th><th>Questões</th><th>Acertos</th><th>Aproveitamento</th></tr></thead><tbody>${d.evolution.map(x=>`<tr><td>${x.sprint}</td><td>${fmtDate(x.date)}</td><td>${x.questions}</td><td>${x.correct}</td><td>${fmtPct(x.accuracy)}</td></tr>`).join('')}</tbody></table></div></section>${footer}`;
 }
 if(page==='riscos'){
  main.innerHTML=`${hero('Diagnóstico','Riscos e erros','Oito erros reais foram registrados no S01. O objetivo é transformar cada falha reaplicável em revisão, regra de prevenção e nova tentativa.')}
  <section class="grid metrics">${metric('Erros registrados',m.errors,'resultado do S01')}${metric('Taxa de erro','22,86%','8 ÷ 35 questões')}${metric('Maior gargalo','Comuns','40% de acerto')}${metric('Sprint vencido','S02','sem execução registrada')}</section>
  <section class="section"><div class="section-head"><div><h2>Erros por bloco</h2><p>Distribuição objetiva confirmada.</p></div></div><article class="card panel">${bars(d.errorsByBlock.map(x=>({label:x.label,value:x.errors})))}</article></section>
  <section class="section"><div class="section-head"><div><h2>Prioridades estratégicas</h2><p>Ações de maior retorno imediato.</p></div></div><div class="grid three">${d.alerts.map(alertCard).join('')}</div></section>${footer}`;
 }
 if(page==='agenda'){
  main.innerHTML=`${hero('Planejamento','Agenda S01–S42',`Há ${remaining} Sprints restantes e ${days} dias até a prova. O ritmo matemático atual é de ${pace.toFixed(2).replace('.',',')} Sprint por dia.`)}
  <section class="grid metrics">${metric('Concluídos',m.completed,`de ${m.total} Sprints`)}${metric('Restantes',remaining,'S02–S42')}${metric('Dias até a prova',days,fmtDate(d.meta.examDate))}${metric('Ritmo necessário',pace.toFixed(2).replace('.',','),'Sprint por dia')}</section>
  <section class="section"><div class="section-head"><div><h2>Próximos marcos</h2><p>Execução real separada do planejamento.</p></div></div><div class="timeline">${d.agenda.map(x=>`<article class="card timeline-item"><span class="timeline-date">${fmtDate(x.date)}</span><div><b>${x.id} — ${x.title}</b><small>${x.detail}</small></div><span class="status ${x.status==='Pendente'?'critical':'warning'}">${x.status}</span></article>`).join('')}</div></section>${footer}`;
 }
 if(page==='casos'){
  main.innerHTML=`${hero('Prova discursiva','Estudos de caso EDAS','Acompanhe prontidão, execução, correção e reescrita dos casos SEC01–SEC12.')}
  <section class="grid metrics">${metric('Casos planejados',m.casesTotal,'SEC01–SEC12')}${metric('Executados',m.casesCompleted,'nenhum texto registrado')}${metric('Pronto e auditado','SEC01','20–30 linhas')}${metric('Próxima ação','Executar SEC01','e realizar D+1')}</section>
  <section class="section"><div class="section-head"><div><h2>Casos programados</h2><p>Primeiros casos do ciclo.</p></div></div><div class="grid two">${d.cases.map(x=>`<article class="card panel"><span class="kicker">${x.id} · ${fmtDate(x.date)}</span><h3>${escapeHTML(x.theme)}</h3><div class="tags"><span class="tag">${x.status}</span><span class="tag">${x.readiness}</span></div><p>${escapeHTML(x.next)}</p></article>`).join('')}</div></section>${footer}`;
 }
 if(page==='auditoria'){
  main.innerHTML=`${hero('Governança','Auditoria e downloads','Consulte as fontes oficiais, verifique as limitações do snapshot e exporte os dados usados pelo painel.')}
  <section class="section"><div class="section-head"><div><h2>Qualidade dos dados</h2><p>Pendências e divergências sem ocultação.</p></div></div><div class="grid three">${d.quality.map(x=>`<article class="card alert" data-level="${x.level}"><span class="alert-icon">${x.level==='critical'?'!':x.level==='warning'?'△':'i'}</span><div><b>${x.title}</b><p>${x.detail}</p></div></article>`).join('')}</div></section>
  <section class="section"><div class="section-head"><div><h2>Fontes oficiais</h2><p>Bancos independentes do TDAS.</p></div></div><div class="grid three">${d.sources.map(x=>`<a class="card source" href="${x.url}" target="_blank" rel="noopener"><small>Notion</small><b>${x.name}</b><span>${x.detail}</span></a>`).join('')}</div></section>
  <section class="section"><div class="section-head"><div><h2>Downloads</h2><p>Arquivos gerados diretamente no navegador.</p></div></div><div class="grid two">${[['sprints','CSV','Execução dos Sprints'],['erros','CSV','Erros por bloco'],['casos','CSV','Estudos de caso'],['qualidade','CSV','Qualidade dos dados'],['snapshot','JSON','Snapshot integral']].map(x=>`<button class="card download" data-download="${x[0]}"><span class="file">${x[1]}</span><span><b>${x[2]}</b><small>Baixar arquivo atualizado</small></span><span>↓</span></button>`).join('')}</div></section>${footer}`;
 }
 if(page==='mais'){
  main.innerHTML=`${hero('Navegação','Mais áreas','Acesse agenda, estudos de caso e auditoria pelo celular.')}
  <section class="section"><div class="grid portal-grid"><a class="card portal" href="${routes.agenda}"><small>Execução</small><b>Agenda</b><span>Próximos Sprints e ritmo necessário.</span><em>Abrir →</em></a><a class="card portal" href="${routes.casos}"><small>Discursiva</small><b>Estudos de caso</b><span>SEC01–SEC12, prontidão e execução.</span><em>Abrir →</em></a><a class="card portal" href="${routes.auditoria}"><small>Governança</small><b>Auditoria</b><span>Fontes, qualidade e downloads.</span><em>Abrir →</em></a></div></section>${footer}`;
 }
 document.querySelectorAll('[data-sync]').forEach(el=>el.textContent=d.meta.syncTimes.join(' · '));
}catch(e){setLoadingError(e)}
