import{BASE,loadData,loadJSON,setupShell,escapeHTML,routes,setLoadingError}from'../common.js?v=20260807.3';
import{readModuleState}from'./module-store.js?v=20260807.3';
const CATALOG_KEY='edas.400.question-catalog.v1',MATERIAL_CACHE='edas-material-user-v1';
async function savedOffline(){if(!('caches'in window))return false;const cache=await caches.open(MATERIAL_CACHE);return Boolean(await cache.match(BASE+'data/integration/daily-material.json',{ignoreSearch:true}))}
async function saveOffline(){if(!('caches'in window))throw new Error('Cache API indisponível.');const cache=await caches.open(MATERIAL_CACHE),resources=[BASE+'estudar/',BASE+'data/site.json',BASE+'data/platform-version.json',BASE+'data/integration/daily-material.json',BASE+'data/integration/question-catalog.json',BASE+'assets/common.js',BASE+'assets/integration/module-dashboard.js',BASE+'assets/edas.css'];for(const resource of resources){const response=await fetch(resource,{cache:'reload'});if(!response.ok)throw new Error(`Falha ao salvar ${resource} (${response.status}).`);await cache.put(resource,response.clone())}}
async function removeOffline(){if(!('caches'in window))return;await caches.delete(MATERIAL_CACHE)}
try{
 const[d,material,officialCatalog]=await Promise.all([loadData(),loadJSON('data/integration/daily-material.json',{optional:true}),loadJSON('data/integration/question-catalog.json',{optional:true})]);setupShell('estudar',d.meta);const current=d.today;let state;try{state=readModuleState()}catch{state={attempts:[],reviews:[],errors:[],aiQueue:[]}}let localCatalog=null;try{localCatalog=JSON.parse(localStorage.getItem(CATALOG_KEY)||'null')}catch{}const activeCatalog=Array.isArray(localCatalog?.questions)&&localCatalog.questions.length?localCatalog:officialCatalog,catalogCount=Array.isArray(activeCatalog?.questions)?activeCatalog.questions.length:0,catalogSource=localCatalog?.questions?.length?'importado neste aparelho':officialCatalog?.questions?.length?'oficial publicado':'indisponível',ready=material?.status==='ready'&&material?.html,offline=await savedOffline();
 document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Execução diária · etapa 1</span><h1>Estudar — ${escapeHTML(current.sprint)}</h1><p>${escapeHTML(current.title)}. O fluxo usa dados exclusivos do Cargo 400 e preserva o último snapshot válido quando o Notion falha.</p><div class="tags"><span class="tag">${escapeHTML(current.status)}</span><span class="tag">${ready?'Material oficial incorporado':'Plano do snapshot preservado'}</span><span class="tag">${catalogCount?`${catalogCount} questões · ${catalogSource}`:'Catálogo autorizado pendente'}</span><span class="tag">${offline?'Material salvo offline':'Offline opcional'}</span></div><div class="hero-actions"><a class="btn primary" href="#material-do-dia">Ler material</a><a class="btn" href="${routes.resolver}">Ir para as questões</a><a class="btn" href="${routes.hoje}">Abrir checklist</a></div></section><section class="section module-sequence"><div class="section-head"><div><h2>Sequência de execução</h2><p>Estude, responda e só então registre a correção.</p></div><span class="stamp">${escapeHTML(current.sprint)}</span></div><div class="grid three"><a class="card portal" href="#material-do-dia"><small>Agora</small><b>Material e revisão</b><span>${escapeHTML(current.review)} · ${escapeHTML(current.material)}</span><em>Ler abaixo ↓</em></a><a class="card portal" href="${routes.resolver}"><small>Depois</small><b>Questões do dia</b><span>${catalogCount?`${catalogCount} itens autorizados disponíveis.`:'Aguardando catálogo oficial autorizado.'}</span><em>Abrir questões →</em></a><a class="card portal" href="${routes.hoje}"><small>Fechar</small><b>Resultado e erros</b><span>Checklist, tempo, observações e fechamento do Sprint.</span><em>Abrir foco →</em></a></div></section><section class="section" id="material-do-dia"><div class="section-head"><div><h2>${ready?'Material oficial do Sprint':'Plano preservado do Sprint'}</h2><p>${ready?'Conteúdo incorporado da fonte oficial.':escapeHTML(material?.notice||'A fonte não foi lida nesta rodada; o último snapshot válido foi mantido.')}</p></div><a class="btn" href="${current.links?.sprint||current.links?.control}" target="_blank" rel="noopener">Conferir no Notion ↗</a></div>${ready?`<article class="card panel daily-material">${material.html}</article>`:`<article class="card panel"><span class="kicker">${escapeHTML(current.sprint)} · ${escapeHTML(current.block)}</span><h2>${escapeHTML(current.title)}</h2><div class="grid two"><div><h3>Revisões</h3><p>${escapeHTML(current.review)}</p><h3>Conhecimentos comuns</h3><p>${escapeHTML(current.common)}</p><h3>Português</h3><p>${escapeHTML(current.portuguese)}</p></div><div><h3>Entregável</h3><p>${escapeHTML(current.deliverable)}</p><h3>Tempo previsto</h3><p>${current.time} minutos</p><h3>Critério objetivo</h3><p>${escapeHTML(current.block)}</p></div></div></article>`}<article class="card panel"><span class="kicker">Uso sem conexão</span><h2>${offline?'Material preservado neste aparelho':'Salvar material para offline'}</h2><p>Esta cópia usa um cache pessoal separado e não é apagada nas atualizações normais do aplicativo.</p><div class="hero-actions"><button class="btn primary" type="button" data-material-offline-save>Salvar para offline</button><button class="btn" type="button" data-material-offline-remove>Remover cópia</button></div><div class="saved" data-material-offline-status></div></article></section><section class="section"><article class="card panel catalog-note"><small>Catálogo autorizado</small><h2>${catalogCount?`${catalogCount} questões disponíveis`:'Questões ainda não incorporadas'}</h2><p>${catalogCount?`Fonte: ${escapeHTML(catalogSource)}. O gabarito continua separado e só é carregado depois da finalização.`:'A integração não copia questões do TDAS nem inventa conteúdo. O catálogo só é usado quando há fonte autorizada do Cargo 400.'}</p><div class="hero-actions"><a class="btn ${catalogCount?'primary':''}" href="${routes.resolver}">${catalogCount?'Iniciar sessão':'Abrir módulo Resolver'}</a><a class="btn" href="${current.links?.sprint||current.links?.control}" target="_blank" rel="noopener">Abrir fonte oficial ↗</a></div></article></section><section class="section"><div class="grid portal-grid"><a class="card portal" href="${routes.revisar}"><small>${state.reviews.filter(x=>x.status==='pending'&&x.dueAt<=Date.now()).length} disponíveis</small><b>Revisar</b><span>Agenda adaptativa e reforços locais.</span><em>Abrir →</em></a><a class="card portal" href="${routes.caderno}"><small>${state.errors.length} erros locais</small><b>Caderno integrado</b><span>Oficial recuperado + registros locais.</span><em>Abrir →</em></a><a class="card portal" href="${routes.desempenho}"><small>${state.attempts.length} tentativas</small><b>Desempenho</b><span>Métricas reconstruídas deste aparelho.</span><em>Abrir →</em></a><a class="card portal" href="${routes.fila}"><small>${state.aiQueue.length} ressalvas</small><b>Fila de IA</b><span>Análise humana obrigatória.</span><em>Abrir →</em></a></div></section><footer class="footer"><span>Material diário · ${escapeHTML(current.sprint)}</span><span>Snapshot <span data-snapshot></span></span></footer>`;
 const status=document.querySelector('[data-material-offline-status]');document.querySelector('[data-material-offline-save]').onclick=async()=>{status.textContent='Salvando…';try{await saveOffline();status.textContent='Material, catálogo e recursos essenciais salvos para uso offline.'}catch(error){status.textContent=error.message}};document.querySelector('[data-material-offline-remove]').onclick=async()=>{await removeOffline();status.textContent='Cópia pessoal do material removida.'};
}catch(error){setLoadingError(error)}
