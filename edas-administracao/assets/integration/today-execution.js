import{loadData,escapeHTML,fmtDateTime}from'../common.js?v=20260807.3';
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
const format=seconds=>{const h=Math.floor(seconds/3600),m=Math.floor(seconds%3600/60),s=Math.floor(seconds%60);return[h,m,s].map(value=>String(value).padStart(2,'0')).join(':')};
try{
 const d=await loadData();for(let attempts=0;attempts<100&&!document.querySelector('[data-today-session]');attempts++)await sleep(40);
 const root=document.querySelector('[data-today-session]');if(!root)throw new Error('Checklist diário indisponível.');
 const key=`edas.400.today.${d.today.sprint}.v1`,timerKey=key+'.timer';
 let state={steps:{},notes:'',closedAt:null,...(JSON.parse(localStorage.getItem(key)||'{}'))};
 let timer={elapsed:0,running:false,startedAt:null,...(JSON.parse(localStorage.getItem(timerKey)||'{}'))};
 if(timer.running&&timer.startedAt)timer.elapsed+=Math.max(0,Math.floor((Date.now()-timer.startedAt)/1000)),timer.startedAt=Date.now();
 const boxes=[...root.querySelectorAll('[data-step-id]')],progress=document.querySelector('[data-session-progress]'),notes=document.querySelector('[data-session-notes]'),saved=document.querySelector('[data-session-saved]'),display=document.querySelector('[data-timer-display]'),closeBtn=document.querySelector('[data-close-local]'),reopenBtn=document.querySelector('[data-reopen-local]'),closeHelp=document.querySelector('[data-close-help]'),status=document.querySelector('[data-local-close-status]'),statusDetail=document.querySelector('[data-local-close-detail]');
 const persist=()=>{localStorage.setItem(key,JSON.stringify(state));localStorage.setItem(timerKey,JSON.stringify(timer));saved.textContent='Salvo neste aparelho';clearTimeout(persist.t);persist.t=setTimeout(()=>saved.textContent='',1600)};
 const allDone=()=>boxes.length>0&&boxes.every(box=>box.checked);
 const refreshClose=()=>{const closed=Boolean(state.closedAt),ready=allDone();if(status)status.textContent=closed?'Fechado localmente':ready?'Pronto para fechar':'Em aberto';if(statusDetail)statusDetail.textContent=closed?`confirmado em ${fmtDateTime(state.closedAt)}`:ready?'todas as etapas marcadas':'checklist local independente';if(closeHelp)closeHelp.textContent=closed?'Este fechamento existe somente neste aparelho. Confira o Notion para o status oficial.':ready?'Todas as etapas estão marcadas. Confirme o fechamento local quando terminar a correção e o registro.':'Marque todas as etapas antes de fechar. O registro oficial continua sendo feito no Notion.';if(closeBtn){closeBtn.disabled=!ready||closed;closeBtn.hidden=closed}if(reopenBtn)reopenBtn.hidden=!closed};
 const refreshProgress=()=>{const done=boxes.filter(box=>box.checked).length;progress.textContent=`${done}/${boxes.length} · ${Math.round(done/Math.max(1,boxes.length)*100)}%`;refreshClose()};
 boxes.forEach(box=>{box.checked=Boolean(state.steps[box.dataset.stepId]);box.addEventListener('change',()=>{state.steps[box.dataset.stepId]=box.checked;if(state.closedAt&&!allDone())state.closedAt=null;refreshProgress();persist()})});
 notes.value=state.notes||'';notes.addEventListener('input',()=>{state.notes=notes.value;persist()});refreshProgress();
 const elapsed=()=>timer.elapsed+(timer.running&&timer.startedAt?Math.floor((Date.now()-timer.startedAt)/1000):0);
 const tick=()=>{display.textContent=format(elapsed())};tick();setInterval(tick,1000);
 document.querySelector('[data-timer-start]')?.addEventListener('click',()=>{if(timer.running)return;timer.running=true;timer.startedAt=Date.now();persist()});
 document.querySelector('[data-timer-pause]')?.addEventListener('click',()=>{if(!timer.running)return;timer.elapsed=elapsed();timer.running=false;timer.startedAt=null;persist();tick()});
 document.querySelector('[data-timer-reset]')?.addEventListener('click',()=>{timer={elapsed:0,running:false,startedAt:null};persist();tick()});
 closeBtn?.addEventListener('click',()=>{if(!allDone())return;if(!confirm('Fechar esta execução somente neste aparelho? Isso não altera o Status no Notion.'))return;if(timer.running){timer.elapsed=elapsed();timer.running=false;timer.startedAt=null}state.closedAt=new Date().toISOString();persist();tick();refreshClose()});
 reopenBtn?.addEventListener('click',()=>{state.closedAt=null;persist();refreshClose()});
 document.querySelector('[data-copy-summary]')?.addEventListener('click',async()=>{const done=boxes.filter(box=>box.checked).map(box=>box.closest('.check')?.querySelector('b')?.textContent).filter(Boolean),pending=boxes.filter(box=>!box.checked).map(box=>box.closest('.check')?.querySelector('b')?.textContent).filter(Boolean),official=(d.sprints||[]).find(item=>item.id===d.today.sprint)?.status||d.today.status;const text=[`${d.today.sprint} — ${d.today.title}`,`Status oficial: ${official||'não informado'}`,`Fechamento local: ${state.closedAt?`sim — ${fmtDateTime(state.closedAt)}`:'não'}`,`Tempo: ${format(elapsed())}`,`Concluído: ${done.join('; ')||'nenhuma etapa'}`,`Pendente: ${pending.join('; ')||'nenhuma etapa'}`,state.notes?`Notas: ${state.notes}`:''].filter(Boolean).join('\n');try{await navigator.clipboard.writeText(text);saved.textContent='Resumo copiado'}catch{saved.textContent=escapeHTML('Não foi possível copiar automaticamente')}});
}catch(error){console.error('Execução diária indisponível',error)}
