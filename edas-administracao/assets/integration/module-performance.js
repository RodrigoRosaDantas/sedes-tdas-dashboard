import{loadData,setupShell,routes,setLoadingError}from'../common.js?v=20260807.1';
import{readModuleState}from'./module-store.js?v=20260807.1';
const pct=value=>`${Number(value||0).toFixed(1).replace('.',',')}%`,duration=ms=>`${Math.floor(Number(ms||0)/60000)}min ${Math.floor(Number(ms||0)%60000/1000)}s`;
try{
 const d=await loadData();setupShell('desempenho',d.meta);const state=readModuleState(),attempts=state.attempts,questions=attempts.reduce((sum,x)=>sum+x.total,0),correct=attempts.reduce((sum,x)=>sum+x.correct,0),elapsed=attempts.reduce((sum,x)=>sum+x.elapsedMs,0),accuracy=questions?correct/questions*100:0;
 document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Desempenho do módulo</span><h1>Desempenho</h1><p>Métricas reconstruídas exclusivamente das sessões locais deste dispositivo.</p><div class="tags"><span class="tag">${attempts.length} tentativas</span><span class="tag">${state.reviews.filter(x=>x.status==='pending'&&x.dueAt<=Date.now()).length} revisões disponíveis</span><span class="tag">Sem writeback</span></div><div class="hero-actions"><a class="btn primary" href="${routes.resolver}">Resolver questões</a><a class="btn" href="${routes.revisar}">Revisar</a><a class="btn" href="${routes.evolucao}">Ver evolução oficial</a></div></section>${attempts.length?`<section class="grid metrics"><article class="card metric"><small>Aproveitamento local</small><strong>${pct(accuracy)}</strong><span>${correct}/${questions} respostas corretas</span></article><article class="card metric"><small>Tentativas</small><strong>${attempts.length}</strong><span>${attempts.filter(x=>x.mode==='study').length} estudo · ${attempts.filter(x=>x.mode==='review').length} revisão</span></article><article class="card metric"><small>Tempo total</small><strong>${duration(elapsed)}</strong><span>${questions?duration(elapsed/questions):'0min 0s'} por questão</span></article><article class="card metric"><small>Melhor resultado</small><strong>${pct(Math.max(...attempts.map(x=>x.percent)))}</strong><span>último: ${pct(attempts[0]?.percent)}</span></article></section><section class="section"><div class="section-head"><div><h2>Últimas tentativas</h2></div></div><div class="grid two">${attempts.slice(0,20).map(item=>`<article class="card panel"><small>${item.mode==='review'?'Revisão':'Estudo'} · ${item.sprintId||'Sessão local'}</small><h3>${item.correct}/${item.total} · ${pct(item.percent)}</h3><p>Tempo: ${duration(item.elapsedMs)}</p></article>`).join('')}</div></section>`:'<section class="section"><article class="card panel"><h2>Nenhuma tentativa local</h2><p>O painel será preenchido quando houver catálogo autorizado e sessões concluídas.</p></article></section>'}<section class="section"><article class="card panel"><h2>Separação preservada</h2><p>Este painel local não altera a Evolução oficial, os Sprints ou o Notion.</p></article></section>`;
}catch(error){setLoadingError(error)}
