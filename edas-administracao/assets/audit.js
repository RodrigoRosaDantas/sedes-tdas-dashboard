import{loadData,loadJSON,setupShell,fmtNumber,fmtDate,fmtDateTime,escapeHTML,routes,RELEASE,setLoadingError}from'./common.js?v=20260807.7';
import{readModuleState,exportModuleState,clearModuleState}from'./integration/module-store.js?v=20260807.7';
const toCSV=rows=>{if(!rows.length)return'';const keys=[...new Set(rows.flatMap(Object.keys))],quote=value=>`"${String(value??'').replace(/"/g,'""')}"`;return[keys.map(quote).join(','),...rows.map(row=>keys.map(key=>quote(row[key])).join(','))].join('\n')};
const download=(name,content,type)=>{const blob=new Blob(['\ufeff'+content],{type}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=name;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)};
try{
 const[d,h,p]=await Promise.all([loadData(),loadJSON('data/sync-history.json',{optional:true}),loadJSON('data/platform-version.json',{optional:true})]);setupShell('auditoria',d.meta);let local;try{local=readModuleState()}catch{local={attempts:[],errors:[],reviews:[],aiQueue:[],marked:[]}}const coverage=d.errorCoverage||{loaded:d.errors?.length||0,total:d.metrics?.errors||0},missing=Math.max(0,Number(coverage.total||0)-Number(coverage.loaded||0));
 document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Governança</span><h1>Auditoria e downloads</h1><p>Consulte fontes, limitações, publicação, histórico e arquivos usados pelo painel.</p><div class="hero-actions"><a class="btn" href="${routes.historico}">Histórico completo</a><a class="btn primary" href="${routes.dados}">Backup dos dados locais</a></div></section><section class="section"><div class="section-head"><div><h2>Qualidade dos dados</h2><p>Pendências e divergências sem ocultação.</p></div></div><div class="grid three">${(d.quality||[]).map(item=>`<article class="card alert" data-level="${item.level}"><span class="alert-icon">${item.level==='critical'?'!':item.level==='warning'?'△':'i'}</span><div><b>${escapeHTML(item.title)}</b><p>${escapeHTML(item.detail)}</p></div></article>`).join('')}${missing?`<article class="card alert" data-level="warning"><span class="alert-icon">△</span><div><b>${missing} erros oficiais sem detalhe individual</b><p>O total ${coverage.total} está preservado; apenas ${coverage.loaded} registros entram em diagnóstico temático.</p></div></article>`:''}</div></section><section class="section"><div class="section-head"><div><h2>Fontes oficiais</h2><p>Bancos independentes do TDAS.</p></div></div><div class="grid three">${(d.sources||[]).map(item=>`<a class="card source" href="${item.url}" target="_blank" rel="noopener"><small>Notion</small><b>${escapeHTML(item.name)}</b><span>${escapeHTML(item.detail)}</span></a>`).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Saúde da plataforma</h2><p>Release técnica separada da versão dos dados.</p></div></div><div class="grid metrics"><article class="card metric"><small>Release técnica</small><strong>${escapeHTML(p?.platformVersion||RELEASE)}</strong><span>PWA e arquitetura</span></article><article class="card metric"><small>Versão dos dados</small><strong>${escapeHTML(p?.dataVersion||d.meta.version||'—')}</strong><span>snapshot ${fmtDate(d.meta.snapshotDate)}</span></article><article class="card metric"><small>Última sincronização</small><strong>${p?.syncAt?fmtDateTime(p.syncAt):fmtDateTime(d.meta.generatedAt)}</strong><span>execução efetiva</span></article><article class="card metric"><small>Cobertura dos erros</small><strong>${fmtNumber(coverage.loaded)}/${fmtNumber(coverage.total)}</strong><span>${missing?'recuperação pendente':'cobertura integral'}</span></article></div></section><section class="section"><div class="section-head"><div><h2>Downloads</h2><p>Arquivos gerados diretamente do snapshot atual.</p></div></div><div class="grid two"><button class="card download" data-download="sprints"><span class="file">CSV</span><span><b>Sprints S01–S42</b><small>Planejamento e execução</small></span><span>↓</span></button><button class="card download" data-download="erros"><span class="file">CSV</span><span><b>Erros detalhados</b><small>Somente registros recuperados</small></span><span>↓</span></button><button class="card download" data-download="casos"><span class="file">CSV</span><span><b>Estudos de caso</b><small>SEC01–SEC12</small></span><span>↓</span></button><button class="card download" data-download="snapshot"><span class="file">JSON</span><span><b>Snapshot integral</b><small>Dados usados pelo painel</small></span><span>↓</span></button><button class="card download" data-download="history"><span class="file">JSON</span><span><b>Histórico</b><small>Sincronizações e falhas</small></span><span>↓</span></button><button class="card download" data-download="local"><span class="file">JSON</span><span><b>Somente módulo local</b><small>Exportação técnica do player</small></span><span>↓</span></button></div></section><section class="section"><div class="grid two"><article class="card panel"><h2>Backup completo do aparelho</h2><p>Inclui módulo de questões, rascunho interrompido, checklists/timers e espaços locais dos SECs.</p><a class="btn primary" href="${routes.dados}">Abrir backup e restauração</a></article><article class="card panel"><h2>Limpeza do módulo</h2><p>Apaga somente tentativas, revisões, erros e fila do player. Faça backup antes.</p><button class="btn danger" type="button" data-clear-local>Apagar dados locais do módulo</button></article></div></section><footer class="footer"><span>Auditoria · ${fmtDate(d.meta.snapshotDate)}</span><span><a href="${routes.mais}">Abrir todos os módulos</a></span></footer>`;
 document.addEventListener('click',event=>{const target=event.target.closest('[data-download]');if(target){const type=target.dataset.download;if(type==='sprints')download('edas-sprints.csv',toCSV(d.sprints||[]),'text/csv;charset=utf-8');if(type==='erros')download('edas-erros.csv',toCSV(d.errors||[]),'text/csv;charset=utf-8');if(type==='casos')download('edas-casos.csv',toCSV(d.cases||[]),'text/csv;charset=utf-8');if(type==='snapshot')download('edas-snapshot.json',JSON.stringify(d,null,2),'application/json;charset=utf-8');if(type==='history')download('edas-sync-history.json',JSON.stringify(h||{},null,2),'application/json;charset=utf-8');if(type==='local')download('edas-modulo-local.json',exportModuleState(),'application/json;charset=utf-8')}if(event.target.closest('[data-clear-local]')){if(confirm('Apagar tentativas, revisões e registros locais do módulo? O restante dos dados locais será preservado.')){clearModuleState();location.reload()}}});
}catch(error){setLoadingError(error)}
