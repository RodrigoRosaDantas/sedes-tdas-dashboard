name: Preservar telemetria local após sync

on:
  workflow_dispatch:
  workflow_run:
    workflows: ['Sincronizar Plataforma TDAS v26']
    types: [completed]

permissions:
  contents: write

concurrency:
  group: tdas-telemetry-pwa-preserve
  cancel-in-progress: false

jobs:
  preserve:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Baixar main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Restaurar assets locais no PWA
        run: node scripts/preserve-local-telemetry-pwa.mjs
      - name: Validar contrato local
        run: node scripts/validate-local-telemetry.mjs
      - name: Publicar somente se o gerador removeu o precache
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- sw.js; then
            echo 'PWA já preserva a telemetria local.'
            exit 0
          fi
          git config user.name 'tdas-telemetry-guard[bot]'
          git config user.email 'tdas-telemetry-guard[bot]@users.noreply.github.com'
          git add sw.js
          git commit -m 'Preservar telemetria local no PWA após sync'
          git push origin HEAD:main
