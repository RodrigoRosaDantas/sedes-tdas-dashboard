import{BASE,loadJSON,setupShell,escapeHTML,fmtNumber,fmtPct,fmtDate,setLoadingError}from'./common.js';
import{readModuleState}from'./integration/module-store.js?v=2.0.0';
import{buildOfficialMentorAnalysis,buildLocalMentorSignals,normalizeText}from'./integration/mentor-engine.js?v=1.1.0';
const META={critical:['Crítico','🔴'],high:['Alto','🟠'],attention:['Atenção','🟡'],controlled:['Controlado','🟢']},safe=v=>escapeHTML(v??'—'),score=v=>`${Math.round(Number(v||0))}/100`,CAUSE_KEY='tdas.202.error-causes.v1';
const readCauses=()=>{try{return JSON.parse(localStorage.getItem(CAUSE_KEY)||'null')||{causes:{}}}catch{return{causes:{}}}};
const age=d=>d==null?'idade desconhecida':d===0?'hoje':d===1?'ontem':`há ${d} dias`;
const reviewUrl=i=>`${BASE}revisar/?mentor=${encodeURIComponent(i.label)}&subject=${encodeURIComponent(i.materia)}${i.edital?.id?`&topic=${encodeURIComponent(i.edital.id)}`:''}`;
const factors=i=>Object.entries(i.breakdown||{}).map(([k,v])=>`<div class="mentor-factor"><span><b>${safe({recurrence:'Reincidência',recency:'Recência',gravity:'Gravidade registrada',trend:'Tendência',edital:'Relevância no edital',review:'Revisão necessária'}[k]||k)}</b><small>${fmtNumber(v.points)}/${fmtNumber(v.max)}</small></span><div><i style="width:${Math.min(100,Number(v.points||0)/Math.max(1,Number(v.max||1))*100)}%"></i></div></div>`).join('');
const card=(i,n)=>{const m=META[i.severity]||META.controlled,dates=[...(i.dates||[])].sort().reverse().slice(0,8);return`<article class="card mentor-priority" data-mentor-priority data-severity="${safe(i.severity)}" data-search="${safe(normalizeText(`${i.materia} ${i.tema} ${i.subtema} ${(i.patterns||[]).join(' ')}`))}"><div class="mentor-priority-head"><div><small>#${n+1} · ${safe(i.materia)}</small><h3>${safe(i.label)}</h3></div><span class="mentor-severity" data-level="${safe(i.severity)}">${m[1]} ${m[0]} · ${score(i.score)}</span></div><p class="mentor-evidence"><strong>${fmtNumber(i.errorCount)} erro${i.errorCount===1?'':'s'}</strong> · reincidência ${fmtNumber(i.recurrence)} · último ${safe(age(i.daysSinceLast))} · tendência ${safe(i.trend?.label||'sem série')}</p><div class="mentor-dates">${dates.map(d=>`<span>${safe(fmtDate(d))}</span>`).join('')}</div><div class="mentor-action"><small>O que eu faria</small><strong>${safe(i.action)}</strong></div><div class="hero-actions"><a class="btn primary" href="${reviewUrl(i)}">Revisão dirigida</a>${i.edital?.url?`<a class="btn" href="${safe(i.edital.url)}" target="_blank" rel="noopener">Tópico do edital ↗</a>`:''}</div><details class="mentor-details"><summary>Por que recebeu esta gravidade?</summary>${factors(i)}<p><strong>Primeiro erro:</strong> ${safe(fmtDate(i.firstErrorDate))} · <strong>último erro:</strong> ${safe(fmtDate(i.lastErrorDate))}. ${i.edital?`Edital: <strong>${safe(i.edital.topic)}</strong> (${i.edital.matchType==='relation'?'vínculo oficial':'correspondência textual'}).`:''}</p><p><strong>Revisão:</strong> ${i.review?.required?'necessária segundo “Precisa revisar?”':'sem sinal ativo em “Precisa revisar?”'}${i.review?.window?` · ${safe(i.review.window)}`:''}.</p></details></article>`};
const strongBlock=i=>`<article class="card mentor-strength"><small>${i.level==='strong'?'Bloco de desempenho forte':'Bloco de desempenho estável'}</small><h3>${safe(i.label)}</h3><strong>${fmtPct(i.accuracy,2)}</strong><p>${safe(i.evidence)}</p><span>Indicador agregado; não equivale sozinho a domínio de um assunto.</span></article>`;
const localStrong=i=>`<article class="card mentor-strength local"><small>Força temática local</small><h3>${safe(i.label)}</h3><strong>${fmtPct(i.accuracy,1)}</strong><p>${fmtNumber(i.correct)}/${fmtNumber(i.total)} corretas · últimas 3 sem erro.</p></article>`;
async function errors(){const index=await loadJSON('data/error-questions/index.json'),parts=await Promise.all((index.parts||[]).map(p=>loadJSON(`data/error-questions/${p.file}`)));return{index,rows:parts.flat()}}
try{
 const[{index,rows},subjects,evolution,edital,home]=await Promise.all([errors(),loadJSON('data/subjects.json'),loadJSON('data/evolution.json'),loadJSON('data/edital-status.json'),loadJSON('data/home.json')]);setupShell('desempenho',home.meta||{});
 const official=buildOfficialMentorAnalysis({errors:rows,subjects,evolution,edital,snapshotDate:home.meta?.snapshotDate,examDate:home.meta?.examDate}),local=buildLocalMentorSignals({state:readModuleState(),causeState:readCauses()}),p=official.priorities,focus=p.filter(i=>['critical','high'].includes(i.severity)),top=p[0],blocks=official.strengths.slice(0,6),localStrengths=local.strengths.slice(0,4);
 document.querySelector('main').innerHTML=`<section class="hero mentor-hero"><span class="kicker">Mentor TDAS · diagnóstico explicável</span><h1>Onde você está vulnerável, o que está estável e o que eu faria agora</h1><p>O cálculo cruza datas, reincidência, gravidade, necessidade ativa de revisão e o Check do Edital. “Revisado?” é histórico; o peso de revisão usa “Precisa revisar?”.</p><div class="hero-actions"><a class="btn primary" href="#prioridades">Ver prioridades</a><a class="btn" href="${BASE}revisar/">Abrir revisões</a></div></section><section class="grid metrics mentor-metrics"><article class="card metric"><small>Críticos</small><strong>${official.summary.critical}</strong></article><article class="card metric"><small>Altos</small><strong>${official.summary.high}</strong></article><article class="card metric"><small>Em atenção</small><strong>${official.summary.attention}</strong></article><article class="card metric"><small>Blocos fortes</small><strong>${blocks.length}</strong><span>indicador agregado</span></article><article class="card metric"><small>Até a prova</small><strong>${official.daysToExam??'—'}</strong></article></section>${top?`<section class="section mentor-now"><div class="section-head"><div><span class="kicker">Faça agora</span><h2>Minha orientação para o próximo bloco</h2></div></div><article class="card mentor-command"><div><span class="mentor-severity" data-level="${safe(top.severity)}">${(META[top.severity]||META.controlled)[1]} ${(META[top.severity]||META.controlled)[0]} · ${score(top.score)}</span><h3>${safe(top.label)}</h3><p>${safe(top.materia)} · ${top.errorCount} erro${top.errorCount===1?'':'s'} · último ${safe(age(top.daysSinceLast))}</p></div><div><small>Ação recomendada</small><strong>${safe(top.action)}</strong><div class="hero-actions"><a class="btn primary" href="${reviewUrl(top)}">Iniciar revisão dirigida</a></div></div></article></section>`:''}<section class="section" id="prioridades"><div class="section-head"><div><h2>Pontos fracos e fragilidades</h2><p>Entrada focada em Crítico + Alto; os demais continuam disponíveis.</p></div><span class="stamp" id="mentor-count">${focus.length} de ${p.length} tópicos</span></div><div class="card mentor-filters"><label>Pesquisar<input id="mentor-search" type="search"></label><label>Gravidade<select id="mentor-severity"><option value="priority">🔥 Prioridade agora</option><option value="">Todas</option><option value="critical">🔴 Crítico</option><option value="high">🟠 Alto</option><option value="attention">🟡 Atenção</option><option value="controlled">🟢 Controlado</option></select></label><label>Matéria<select id="mentor-subject"><option value="">Todas</option>${[...new Set(p.map(i=>i.materia))].sort().map(v=>`<option value="${safe(v)}">${safe(v)}</option>`).join('')}</select></label><button class="btn" id="mentor-clear">Voltar ao foco</button></div><div class="mentor-priority-list" id="mentor-list">${p.map(card).join('')}</div><div class="empty" id="mentor-empty" hidden>Nenhuma fragilidade corresponde aos filtros.</div></section><section class="section"><div class="section-head"><div><h2>Blocos de desempenho forte</h2><p>Consistência agregada; não é usada para afirmar domínio de assunto.</p></div></div><div class="grid three mentor-strength-grid">${blocks.map(strongBlock).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Forças temáticas deste dispositivo</h2><p>Exigem amostra local positiva e últimas observações consistentes.</p></div></div><div class="grid three mentor-strength-grid">${localStrengths.map(localStrong).join('')||'<div class="empty">Amostra local ainda insuficiente.</div>'}</div></section><section class="section"><div class="section-head"><div><h2>Linha do tempo dos erros</h2><p>Datas reais do Caderno oficial.</p></div></div><div class="card mentor-timeline">${official.timeline.map(i=>`<article class="mentor-timeline-row"><time>${safe(fmtDate(i.date))}</time><span class="mentor-timeline-dot" data-gravity="${safe(String(i.gravity||'').toLowerCase())}"></span><div><b>${safe(i.subtema||i.tema||i.materia)}</b><small>${safe(i.materia)}${i.origin?` · ${safe(i.origin)}`:''} · ${safe(i.gravity)}</small></div>${i.url?`<a href="${safe(i.url)}" target="_blank" rel="noopener">Notion ↗</a>`:''}</article>`).join('')}</div></section><section class="section"><article class="card mentor-method"><span class="kicker">Como a gravidade é calculada</span><h2>Sem nota secreta</h2><p>${safe(official.methodology.severity)}</p><div class="mentor-formula"><span>Reincidência <b>25</b></span><span>Recência <b>20</b></span><span>Gravidade <b>15</b></span><span>Tendência <b>15</b></span><span>Edital <b>15</b></span><span>Revisão ativa <b>10</b></span></div></article></section><footer class="footer"><span>Mentor TDAS · Cargo 202</span><span>${index.total||rows.length} erros oficiais</span></footer>`;
 const q=document.querySelector('#mentor-search'),sev=document.querySelector('#mentor-severity'),sub=document.querySelector('#mentor-subject'),list=document.querySelector('#mentor-list'),count=document.querySelector('#mentor-count'),empty=document.querySelector('#mentor-empty'),params=new URLSearchParams(location.search);q.value=params.get('q')||'';sub.value=params.get('subject')||'';sev.value=params.has('severity')?params.get('severity'):(params.has('q')||params.has('subject')?'':'priority');
 const update=()=>{const text=normalizeText(q.value),s=sev.value,m=sub.value;let n=0;for(const el of list.querySelectorAll('[data-mentor-priority]')){const sm=!s||(s==='priority'?['critical','high'].includes(el.dataset.severity):el.dataset.severity===s),show=(!text||el.dataset.search.includes(text))&&sm&&(!m||el.querySelector('small')?.textContent?.includes(m));el.hidden=!show;if(show)n++}count.textContent=`${n} de ${p.length} tópicos`;empty.hidden=n>0};for(const c of[q,sev,sub])c.addEventListener(c===q?'input':'change',update);document.querySelector('#mentor-clear').addEventListener('click',()=>{q.value='';sev.value='priority';sub.value='';update()});update();
}catch(error){setLoadingError(error)}
