
import{loadJSON,setupShell,fmtDate,fmtNumber,metric,escapeHTML,setLoadingError}from'./common.js';
try{const d=await loadJSON('data/agenda.json');setupShell('agenda',d.meta);document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Planejamento executável</span><h1>Agenda</h1><p>O próximo passo já está definido. Use os filtros para enxergar apenas a janela de execução necessária.</p></section><section class="grid metrics">${metric('PE restantes',d.summary.remainingPE,`${d.summary.operationalDays} dias operacionais`)}${metric('Ritmo necessário',d.summary.pace.toFixed(2).replace('.',','),'PE por dia')}${metric('Questões planejadas',fmtNumber(d.summary.plannedQuestionsMidpoint),'estimativa pelos pontos médios')}${metric('PE atual',d.current.pe,d.current.status)}</section><section class="section"><div class="section-head"><div><h2>Próximos PE</h2><p>Planejamento futuro separado dos resultados já realizados.</p></div></div><div class="toolbar"><label>Janela<select id="window"><option value="7">Próximos 7</option><option value="14" selected>Próximos 14</option><option value="all">Todo o restante</option></select></label><label>Tipo<select id="type"><option value="all">Todos</option>${[...new Set(d.allFuture.map(x=>x.type))].map(x=>`<option>${escapeHTML(x)}</option>`).join('')}</select></label></div><div class="timeline" id="timeline"></div></section><section class="section"><div class="section-head"><div><h2>Últimos dias concluídos</h2><p>Contexto rápido antes de avançar.</p></div></div><div class="table-wrap"><table><thead><tr><th>PE</th><th>Data</th><th>Atividade</th><th>Meta</th><th>Resultado</th></tr></thead><tbody>${d.recentCompleted.map(x=>`<tr><td>${x.pe}</td><td>${fmtDate(x.date)}</td><td>${escapeHTML(x.title)}</td><td>${x.meta}</td><td>${x.acertos??'Pendente'}${x.accuracy!=null?` · ${x.accuracy.toFixed(2).replace('.',',')}%`:''}</td></tr>`).join('')}</tbody></table></div></section><section class="section"><article class="card formula"><small>Fórmula do ritmo</small><strong>${d.summary.pace.toFixed(2).replace('.',',')} PE/dia</strong><code>${d.summary.remainingPE} PE não concluídos ÷ ${d.summary.operationalDays} dias operacionais inclusivos</code></article></section><footer class="footer"><span>Agenda · planejamento oficial</span><span>Snapshot <span data-snapshot></span></span></footer>`;
 const win=document.querySelector('#window'),type=document.querySelector('#type'),tl=document.querySelector('#timeline');function render(){let rows=d.allFuture.filter(x=>type.value==='all'||x.type===type.value);if(win.value!=='all')rows=rows.slice(0,Number(win.value));tl.innerHTML=rows.map(x=>`<article class="card timeline-item"><span class="timeline-date">${fmtDate(x.date)}</span><div><b>${x.pe} · ${escapeHTML(x.title)}</b><small>${escapeHTML(x.type)} · ${escapeHTML(x.planned_questions)} questões${x.rd?' · '+x.rd:''}</small></div><span class="status warning">${escapeHTML(x.status)}</span></article>`).join('')||'<div class="empty">Nenhum PE para o filtro.</div>'}win.onchange=type.onchange=render;render();
}catch(e){setLoadingError(e)}
