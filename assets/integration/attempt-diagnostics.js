import {escapeHTML} from '../common.js?v=26.17.0';
import {readModuleState} from './module-store.js?v=2.1.0';
const main=document.querySelector('main');
if(!document.querySelector('link[data-attempt-diagnostics-css]')){const link=document.createElement('link');link.rel='stylesheet';link.href='/sedes-tdas-dashboard/assets/integration/attempt-diagnostics.css?v=1.0.0';link.dataset.attemptDiagnosticsCss='1';document.head.append(link)}
const confidence=value=>value==='guess'?'Chute':value==='doubt'?'Dúvida':'Segurança';
const attention=q=>!q.correct||q.confidence!=='secure'||q.marked||q.issue!=='none';
function alternatives(q){return Object.entries(q.alternativas||{}).map(([key,value])=>`<li class="${key===q.correctAnswer?'correct':''} ${key===q.selected&&!q.correct?'selected-wrong':''}"><strong>${escapeHTML(key)})</strong> ${escapeHTML(value)}</li>`).join('')}
function card(q){return `<details class="card panel attempt-question ${q.correct?'is-correct':'is-wrong'}" ${!q.correct?'open':''} data-attempt-card data-filter="${q.correct?'correct':'wrong'} ${attention(q)?'attention':''}"><summary><span><small>Questão ${escapeHTML(q.numeroOriginal??q.id)} · ${escapeHTML(q.materia||q.assunto||'Sem matéria')}</small><strong>${escapeHTML(q.subassunto||q.assunto||'Questão')}</strong></span><b>${q.correct?'✓ Correta':'✕ Incorreta'}</b></summary><div class="attempt-question-body">${q.textoBase?`<blockquote>${escapeHTML(q.textoBase)}</blockquote>`:''}<p class="attempt-stem">${escapeHTML(q.enunciado||'Enunciado indisponível.')}</p><ol class="attempt-options">${alternatives(q)}</ol><div class="attempt-answer-grid"><div class="attempt-answer ${q.correct?'right':'wrong'}"><small>Sua resposta</small><strong>${escapeHTML(q.selected||'—')}</strong>${q.selectedText?`<p>${escapeHTML(q.selectedText)}</p>`:''}</div><div class="attempt-answer right"><small>Gabarito</small><strong>${escapeHTML(q.correctAnswer||'—')}</strong>${q.correctText?`<p>${escapeHTML(q.correctText)}</p>`:''}</div></div><div class="attempt-signals"><span>${confidence(q.confidence)}</span>${q.marked?'<span>Marcada nesta sessão</span>':''}${q.issue!=='none'?'<span>Ressalva editorial</span>':''}</div></div></details>`}
async function render(){if(!main||main.querySelector('[data-attempt-diagnostics]'))return false;const hero=main.querySelector('.pilot-result');if(!hero)return false;const attempt=readModuleState().attempts[0];if(!attempt||attempt.mode!=='study')return false;const heroText=hero.querySelector('p');if(heroText)heroText.textContent='Resultado disponível para correção agora. Esta tentativa concluída não será salva como histórico pessoal nem enviada para a nuvem.';const questions=attempt.questionResults||[],wrong=questions.filter(q=>!q.correct&&q.issue==='none'),attentionItems=questions.filter(attention),uncertain=questions.filter(q=>q.correct&&q.confidence!=='secure'),marked=questions.filter(q=>q.marked);const section=document.createElement('section');section.className='section attempt-diagnostics';section.dataset.attemptDiagnostics='1';section.innerHTML=`<div class="section-head"><div><span class="kicker">Diagnóstico desta bateria</span><h2>Correção disponível enquanto esta página estiver aberta</h2><p>O TDAS não salva este resultado como histórico pessoal. Use a correção agora; ao sair, a tentativa concluída não será restaurada nem enviada para a nuvem.</p></div></div><div class="attempt-summary-grid"><article class="card"><small>Erros</small><strong>${wrong.length}</strong></article><article class="card"><small>Acertos inseguros</small><strong>${uncertain.length}</strong></article><article class="card"><small>Marcadas</small><strong>${marked.length}</strong></article><article class="card"><small>Precisam de atenção</small><strong>${attentionItems.length}</strong></article></div><div class="attempt-filters" role="group" aria-label="Filtrar questões"><button class="btn primary" data-attempt-filter="attention">Atenção (${attentionItems.length})</button><button class="btn" data-attempt-filter="wrong">Erros (${wrong.length})</button><button class="btn" data-attempt-filter="all">Todas (${questions.length})</button></div><div class="attempt-list">${questions.map(card).join('')}</div>`;hero.after(section);const filter=value=>{for(const item of section.querySelectorAll('[data-attempt-card]'))item.hidden=value!=='all'&&!String(item.dataset.filter).split(' ').includes(value);for(const button of section.querySelectorAll('[data-attempt-filter]'))button.classList.toggle('primary',button.dataset.attemptFilter===value)};section.addEventListener('click',event=>{const f=event.target.closest('[data-attempt-filter]');if(f)filter(f.dataset.attemptFilter)});filter(attentionItems.length?'attention':'all');return true}
let busy=false;const tryRender=()=>{if(busy)return;busy=true;render().catch(error=>console.warn('Diagnóstico pós-questões:',error)).finally(()=>{busy=false})};new MutationObserver(tryRender).observe(main||document.body,{childList:true,subtree:true});tryRender();
