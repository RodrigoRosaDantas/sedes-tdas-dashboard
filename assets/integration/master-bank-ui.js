import {BASE} from '../common.js?v=26.17.0';
async function loadSummary(){try{const response=await fetch(BASE+'data/integration/master-question-bank.json',{cache:'no-store'});if(!response.ok)return null;const snapshot=await response.json();if(snapshot?.mode!=='tdas-master-question-bank'||!Number.isFinite(Number(snapshot.questionCount)))return null;return{questions:Number(snapshot.questionCount),sourceQuestions:Number(snapshot.sourceQuestionCount??snapshot.questionCount),excludedAnnulled:Number(snapshot.excludedAnnulledCount||0),materials:Number(snapshot.materialCount)||0,targetCargo:Number(snapshot.targetCargo?.questionCount)||null,targetCargoSource:Number(snapshot.targetCargo?.sourceQuestionCount??snapshot.targetCargo?.questionCount)||null,targetCargoAnnulled:Number(snapshot.targetCargo?.excludedAnnulledCount||0),scope:String(snapshot.scope||''),commit:String(snapshot.source?.commit||snapshot.source?.releaseFingerprint||'').slice(0,7)}}catch{return null}}
function decorate(summary){const totalNode=document.querySelector('[data-bank-total]'),card=document.querySelector('.bank-filter-card');if(!summary||!totalNode||!card||card.querySelector('[data-master-bank-summary]'))return false;const total=Number(String(totalNode.textContent||'').match(/\d+/)?.[0]||0),other=Math.max(0,total-summary.questions),note=document.createElement('p');note.className='bank-note';note.dataset.masterBankSummary='1';const published=summary.sourceQuestions===summary.questions?`${summary.questions} questões resolvíveis`:`${summary.sourceQuestions} questões publicadas · ${summary.questions} resolvíveis · ${summary.excludedAnnulled} anulada${summary.excludedAnnulled===1?'':'s'} preservada${summary.excludedAnnulled===1?'':'s'} na auditoria e excluída${summary.excludedAnnulled===1?'':'s'} das baterias`;const cargo=summary.targetCargo!=null?` · Cargo 202: ${summary.targetCargo}${summary.targetCargoAnnulled?` resolvíveis de ${summary.targetCargoSource} publicadas (${summary.targetCargoAnnulled} anulada${summary.targetCargoAnnulled===1?'':'s'})`:' identificadas diretamente'}`:'';note.innerHTML=`<strong>Acervo publicado:</strong> ${published} em ${summary.materials} materiais${cargo}${other?` + ${other} questão${other===1?'':'ões'} do ciclo/arquivo local`:''}. Use o filtro “Cargo de origem” quando quiser restringir o treino.`;if(summary.commit)note.title=`Snapshot técnico do Banco publicado · ${summary.commit}`;const grid=card.querySelector('.bank-filter-grid');grid?.before(note);return true}
const summary=await loadSummary();if(summary){const observer=new MutationObserver(()=>{if(decorate(summary))observer.disconnect()});observer.observe(document.querySelector('main')||document.body,{childList:true,subtree:true});decorate(summary)}
