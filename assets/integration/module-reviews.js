import {BASE, escapeHTML, loadJSON, setupShell, setLoadingError} from '../common.js?v=24.1';
import {readModuleState} from './module-store.js?v=2.1.0';
import {outcomeLabel,sortReviewsByPriority} from './review-engine.js?v=1.0.0';

const formatDate = value => new Intl.DateTimeFormat('pt-BR', {dateStyle: 'short', timeStyle: 'short'}).format(new Date(value));
const signalLabel=item=>item.sourceOutcome==='wrong_again'?'Novo erro':item.sourceOutcome==='unsure'?'Dúvida persistente':item.classification==='incorrect_confirmed'?'Erro confirmado':item.classification==='correct_by_guess'?'Acerto por chute':item.classification==='correct_with_doubt'?'Acerto com dúvida':item.marked?'Marcada':'Revisão programada';
try {
  const shell = await loadJSON('data/more.json');
  setupShell('mais', shell.meta);
  const now = Date.now();
  const reviews = [...readModuleState().reviews];
  const due = sortReviewsByPriority(reviews.filter(item => item.status === 'pending' && item.dueAt <= now),now);
  const upcoming = reviews.filter(item => item.status === 'pending' && item.dueAt > now).sort((a,b)=>a.dueAt-b.dueAt);
  const completed = reviews.filter(item => item.status === 'completed').sort((a,b)=>Number(b.completedAt||0)-Number(a.completedAt||0));
  const card = item => `<article class="card panel review-card"><small>${escapeHTML(item.stage)} · ${item.peId ? escapeHTML(item.peId) : 'Sessão local'} · questão ${item.numeroOriginal ?? '—'}</small><h3>${escapeHTML(item.subassunto || item.assunto || 'Questão')}</h3><div class="tags"><span class="tag">${escapeHTML(signalLabel(item))}</span>${Number(item.recurrenceCount||0)>0?`<span class="tag">Reincidência ${Number(item.recurrenceCount)}</span>`:''}</div><p>${item.status === 'completed' ? `${escapeHTML(outcomeLabel(item.outcome))} · concluída em ${formatDate(item.completedAt)}` : item.dueAt <= now ? 'Disponível agora · priorizada por risco e atraso' : `Agendada para ${formatDate(item.dueAt)}`}</p>${item.status === 'pending' && item.dueAt <= now ? `<a class="btn primary" href="${BASE}resolver/?review=${encodeURIComponent(item.id)}">Revisar questão</a>` : ''}</article>`;
  document.querySelector('main').innerHTML = `<section class="hero"><span class="kicker">Revisão adaptativa local</span><h1>Revisar</h1><p>A agenda combina D+1, D+7 e D+20 com reforços automáticos após dúvida persistente ou novo erro.</p><div class="tags"><span class="tag">${due.length} disponíveis</span><span class="tag">${upcoming.length} futuras</span><span class="tag">${completed.length} concluídas</span></div><div class="hero-actions"><a class="btn primary" href="${due[0] ? `${BASE}resolver/?review=${encodeURIComponent(due[0].id)}` : `${BASE}resolver/`}">${due.length ? 'Iniciar revisão prioritária' : 'Abrir Resolver'}</a><a class="btn" href="${BASE}caderno-erros/">Abrir caderno</a></div></section><section class="section"><div class="section-head"><div><h2>Disponíveis agora</h2><p>Reincidências e novos erros aparecem antes das revisões de menor risco.</p></div></div>${due.length ? `<div class="grid two">${due.map(card).join('')}</div>` : '<article class="card panel"><p>Nenhuma revisão disponível neste dispositivo.</p></article>'}</section><section class="section"><div class="section-head"><div><h2>Próximas revisões</h2></div></div>${upcoming.length ? `<div class="grid two">${upcoming.map(card).join('')}</div>` : '<article class="card panel"><p>Nenhuma revisão futura.</p></article>'}</section><section class="section"><div class="section-head"><div><h2>Concluídas</h2><p>Resultado pedagógico registrado após a correção.</p></div></div>${completed.length ? `<div class="grid two">${completed.slice(0,20).map(card).join('')}</div>` : '<article class="card panel"><p>Nenhuma revisão concluída.</p></article>'}</section><footer class="footer"><span>Revisões adaptativas · dados locais</span><span>Sem writeback</span></footer>`;
} catch (error) {
  setLoadingError(error);
}
