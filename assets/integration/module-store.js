import {consumeCompletedTelemetry,readTelemetryState,summarizeTelemetry} from './question-telemetry.js';

const STORAGE_KEY='tdas.202.question-module.v2.state';
const SCHEMA_VERSION='2.1.0-local-session-only';
const emptyState=()=>({schemaVersion:SCHEMA_VERSION,updatedAt:null,attempts:[],errors:[],marked:[],reviews:[],aiQueue:[]});
let ephemeralState=freezeState(emptyState());
function resolveStorage(storage){const target=storage??globalThis.localStorage;if(!target||typeof target.getItem!=='function')throw new TypeError('Armazenamento local indisponível.');return target}
function freezeState(state){return Object.freeze({...state,attempts:Object.freeze([...state.attempts]),errors:Object.freeze([...state.errors]),marked:Object.freeze([...state.marked]),reviews:Object.freeze([]),aiQueue:Object.freeze([...state.aiQueue])})}
// Compatibilidade: o histórico legado pode continuar fisicamente no navegador, mas não é mais lido como estado operacional. O único dado persistido durante a resolução é o rascunho da sessão em andamento, responsabilidade de session-draft.js. O resultado final existe apenas em memória enquanto a página atual permanecer aberta.
export function readModuleState(){return ephemeralState}
function classify(result,meta={}){const confidence=['secure','doubt','guess'].includes(meta.confidence)?meta.confidence:'secure',issue=['none','annulment_pending','source_error'].includes(meta.issue)?meta.issue:'none',marked=meta.marked===true;if(issue!=='none')return{classification:issue,confidence,issue,marked};if(!result.correct)return{classification:'incorrect_confirmed',confidence,issue,marked};if(marked)return{classification:'marked',confidence,issue,marked};if(confidence==='doubt')return{classification:'correct_with_doubt',confidence,issue,marked};if(confidence==='guess')return{classification:'correct_by_guess',confidence,issue,marked};return{classification:'correct_secure',confidence,issue,marked}}
function telemetryRecord(value){if(!value)return null;return Object.freeze({activeMs:Math.max(0,Number(value.activeMs)||0),visits:Math.max(0,Number(value.visits)||0),answerChanges:Math.max(0,Number(value.answerChanges)||0),firstAnswer:value.firstAnswer||null,lastAnswer:value.lastAnswer||null,firstAnsweredAt:value.firstAnsweredAt??null,lastAnsweredAt:value.lastAnsweredAt??null})}
const plain=value=>value==null?null:String(value),copy=value=>value&&typeof value==='object'?JSON.parse(JSON.stringify(value)):value;
function questionSnapshot(question,result,catalog={}){const alternativas=copy(question.alternativas||null),selected=result.selected??null,correctAnswer=result.correctAnswer??null;return Object.freeze({codigo:plain(question.codigo||question.id),numeroOriginal:question.numeroOriginal??question.numero_original??question.numero??null,materia:plain(question.materia||question.disciplina||''),assunto:plain(question.assunto||'Sem assunto'),subassunto:plain(question.subassunto||''),textoBase:plain(question.texto_base??question.textoBase),enunciado:plain(question.enunciado),alternativas,selectedText:alternativas?.[selected]??null,correctText:alternativas?.[correctAnswer]??null,sourcePe:plain(question.sourcePe||catalog.peId),sourceTitle:plain(question.sourceTitle||catalog.title),sourceKind:plain(question.sourceKind||catalog.authorizedSource?.type),sourceKeyRef:plain(question.sourceKeyRef||catalog.keyRef),sourceKeyPath:plain(question.sourceKeyPath||catalog.keyPath),sourcePublicPath:plain(question.sourcePublicPath),materialId:plain(question.materialId),materialName:plain(question.materialName),tipoMaterial:plain(question.tipoMaterial||question.tipo_material),banca:plain(question.banca||question.fonte),ano:Number(question.ano)||null,orgao:plain(question.orgao),cargo:plain(question.cargo),codigoCargo:plain(question.codigoCargo||question.codigo_cargo),dificuldade:plain(question.dificuldade),formatoQuestao:plain(question.formatoQuestao||question.formato_questao)})}
function questionRecord(question,result,meta,telemetry,catalog){const classified=classify(result,meta),snapshot=questionSnapshot(question,result,catalog);return Object.freeze({id:question.id,...snapshot,selected:result.selected,correctAnswer:result.correctAnswer,correct:result.correct,...classified,telemetry:telemetryRecord(telemetry)})}
function findTelemetry(catalog,evaluation,storage){const store=readTelemetryState(storage),startedAt=Number(evaluation.session.startedAt)||0,catalogId=String(catalog.catalogId),match=(store.completed||[]).filter(item=>item.catalogId===catalogId&&Math.abs(Number(item.startedAt)-startedAt)<=2500).sort((a,b)=>Math.abs(Number(a.startedAt)-startedAt)-Math.abs(Number(b.startedAt)-startedAt))[0];return match?summarizeTelemetry(match):null}
export function saveCompletedAttempt({catalog,evaluation,responseMeta={},mode='study'},storage){if(!catalog||!evaluation||mode!=='study')throw new TypeError('O TDAS não executa nem persiste revisão interna.');const target=resolveStorage(storage),telemetry=findTelemetry(catalog,evaluation,target),telemetryById=new Map(Object.entries(telemetry?.questions||{})),byId=new Map(catalog.questions.map(question=>[question.id,question])),results=evaluation.results.map(result=>{const question=byId.get(result.id);if(!question)throw new Error(`Questão ausente do catálogo: ${result.id}.`);return questionRecord(question,result,responseMeta[result.id],telemetryById.get(result.id),catalog)}),attempt=Object.freeze({schemaVersion:SCHEMA_VERSION,id:`ephemeral:${catalog.catalogId}:${evaluation.session.startedAt}`,mode:'study',catalogId:catalog.catalogId,peId:catalog.peId||null,startedAt:evaluation.session.startedAt,finishedAt:evaluation.session.finishedAt,elapsedMs:evaluation.elapsedMs,activeElapsedMs:telemetry?.activeElapsedMs??null,telemetryVersion:telemetry?.schemaVersion??null,telemetryQuestionCount:telemetry?.measuredQuestions??0,answerChangeCount:telemetry?.answerChanges??0,revisitCount:telemetry?.revisitEvents??0,correct:evaluation.correct,incorrect:evaluation.incorrect,total:evaluation.total,percent:evaluation.percent,localOnly:true,persistent:false,cloudSync:false,notionWriteback:false,questionResults:Object.freeze(results)}),errors=results.filter(item=>item.classification==='incorrect_confirmed').map(item=>({...item,id:`error:${attempt.id}:${item.id}`,attemptId:attempt.id,peId:attempt.peId,createdAt:attempt.finishedAt})),marked=results.filter(item=>item.marked).map(item=>({...item,id:`marked:${attempt.id}:${item.id}`,attemptId:attempt.id,peId:attempt.peId,createdAt:attempt.finishedAt})),aiQueue=results.filter(item=>['annulment_pending','source_error'].includes(item.classification)).map(item=>({...item,id:`ai:${attempt.id}:${item.id}`,attemptId:attempt.id,peId:attempt.peId,createdAt:attempt.finishedAt,status:'ephemeral'}));ephemeralState=freezeState({schemaVersion:SCHEMA_VERSION,updatedAt:Date.now(),attempts:[attempt],errors,marked,reviews:[],aiQueue});if(telemetry)try{consumeCompletedTelemetry({catalogId:catalog.catalogId,startedAt:evaluation.session.startedAt},target)}catch{}return Object.freeze({attempt,state:ephemeralState,reinforcement:null})}
export function clearModuleState(storage){ephemeralState=freezeState(emptyState());const target=resolveStorage(storage);target.removeItem?.(STORAGE_KEY)}
export {STORAGE_KEY};
