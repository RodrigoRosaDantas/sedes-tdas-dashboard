import {BASE,escapeHTML,loadJSON,setupShell,setLoadingError} from '../common.js?v=26.17.0';
import {readModuleState} from './module-store.js?v=2.1.0';
import {getAttemptExport,downloadAttemptExport,copyAttemptSummary} from './attempt-history.js?v=1.0.0';
import {loadCurrentQuestionSource} from './persistence-local.js?v=1.0.0';
import {buildStudyAnalytics} from './study-analytics.js?v=1.1.0';
try{
 const shell=await loadJSON('data/more.json');setupShell('mais',shell.meta);const state=readModuleState(),params=new URLSearchParams(location.search),id=params.get('id')||state.attempts[0]?.id;if(!id)throw new Error('Nenhuma tentativa disponível.');const catalog=await loadCurrentQuestionSource().catch(()=>null),data=await getAttemptExport(id,{catalog});if(!data)throw new Error('Tentativa não encontrada.');const analytics=buildStudyAnalytics({attempts:state.attempts,reviews:state.reviews}),main=document.querySelector('main');
 main.innerHTML=`<section class="hero"><span class="kicker">Schema ${escapeHTML(data.schemaVersion)}</span><h1>${escapeHTML(data.attempt.peId||'Tentativa')} · ${data.attempt.correct}/${data.attempt.total}</h1><p>${Number(data.attempt.percent).toFixed(1).replace('.',',')}% · ${data.questions.length} registros questão por questão.</p><div class="hero-actions"><button class="btn primary" type="button" data-download>Exportar dados para ChatGPT</button><button class="btn" type="button" data-copy>Copiar resumo para ChatGPT</button><a class="btn" href="${BASE}desempenho/">Voltar ao desempenho</a></div><p data-message aria-live="polite"></p></section><section class="section"><div class="section-head"><div><h2>Questão por questão</h2><p>Conteúdo e telemetria preservados no arquivo estruturado.</p></div></div><pre style="white-space:pre-wrap;overflow:auto" data-json></pre></section>`;main.querySelector('[data-json]').textContent=JSON.stringify(data.questions,null,2);main.querySelector('[data-download]').onclick=()=>downloadAttemptExport(data);main.querySelector('[data-copy]').onclick=async()=>{await copyAttemptSummary(data,{risk:analytics.topics.slice(0,10)});main.querySelector('[data-message]').textContent='Resumo completo copiado.'};
}catch(error){setLoadingError(error)}
