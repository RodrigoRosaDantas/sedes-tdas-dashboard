
import{loadJSON,setupShell,fmtDate,metric,escapeHTML,setLoadingError}from'./common.js';
try{const d=await loadJSON('data/redactions.json');setupShell('redacoes',d.meta);document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Preparação discursiva</span><h1>Redações</h1><p>Ritmo, temas e vínculo com os PE — sem preencher status individual que a fonte não exportou.</p></section><section class="grid metrics">${metric('Redações válidas',d.summary.valid,'RD01–RD32')}${metric('Diagnosticadas',d.summary.corrected,'total agregado confirmado')}${metric('Não iniciadas',d.summary.notStarted,'total agregado confirmado')}${metric('Ritmo necessário',d.summary.perWeek.toFixed(1).replace('.',','),`por semana em ${d.summary.weeksRemaining.toFixed(1).replace('.',',')} semanas`)}</section><section class="section"><article class="card panel"><h3>Transparência dos dados</h3><p>${escapeHTML(d.notice)}</p></article></section><section class="section"><div class="section-head"><div><h2>Banco discursivo</h2><p>Pesquise por tema, RD, PE ou semana.</p></div></div><div class="toolbar"><label>Pesquisar<input id="search" type="search" placeholder="Ex.: SUAS, RD21, patrimônio"></label><label>Semana<select id="week"><option value="all">Todas</option>${[...new Set(d.redactions.map(x=>x.week))].map(x=>`<option value="${x}">Semana ${x}</option>`).join('')}</select></label></div><div class="table-wrap"><table><thead><tr><th>RD</th><th>Semana</th><th>PE</th><th>Data</th><th>Tema</th><th>Status individual</th></tr></thead><tbody id="rows"></tbody></table></div></section><section class="section"><article class="card formula"><small>Fórmula do ritmo discursivo</small><strong>${d.summary.perWeek.toFixed(1).replace('.',',')} redações/semana</strong><code>${d.summary.notStarted} redações não iniciadas ÷ ${d.summary.weeksRemaining.toFixed(1)} semanas restantes</code></article></section><footer class="footer"><span>Redações · banco oficial</span><span>Snapshot <span data-snapshot></span></span></footer>`;
 const s=document.querySelector('#search'),w=document.querySelector('#week'),rows=document.querySelector('#rows');function render(){const q=s.value.toLowerCase().trim();const data=d.redactions.filter(x=>(w.value==='all'||String(x.week)===w.value)&&(!q||[x.rd,x.pe,x.theme,String(x.week)].join(' ').toLowerCase().includes(q)));rows.innerHTML=data.map(x=>`<tr><td>${x.rd}</td><td>${x.week}</td><td>${x.pe}</td><td>${fmtDate(x.date)}</td><td>${escapeHTML(x.theme)}</td><td><span class="status warning">Não exportado</span></td></tr>`).join('')||'<tr><td colspan="6" class="empty">Nenhuma redação encontrada.</td></tr>'}s.oninput=w.onchange=render;render();
}catch(e){setLoadingError(e)}
