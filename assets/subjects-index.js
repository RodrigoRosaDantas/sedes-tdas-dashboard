
import{loadJSON,setupShell,fmtNumber,fmtPct,escapeHTML,setLoadingError}from'./common.js';
const BASE='/sedes-tdas-dashboard/';
try{const d=await loadJSON('data/subjects.json');setupShell('riscos',d.meta);document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Diagnóstico consolidado</span><h1>Matérias</h1><p>Abra a página de cada matéria para consultar padrões dominantes, criticidade, reincidência e recomendação.</p></section><section class="section"><div class="grid portal-grid">${d.subjects.map(s=>`<a class="card subject-card" href="${BASE}materias/${s.slug}/"><small>${fmtNumber(s.errors)} erros · ${fmtPct(s.errors?s.high_critical/s.errors*100:0,1)} críticos/altos</small><b>${escapeHTML(s.subject)}</b><span>${fmtNumber(s.recurrent)} reincidentes · ${fmtNumber(s.flashcards)} flashcards</span><em>Abrir diagnóstico →</em></a>`).join('')}</div></section><footer class="footer"><span>Matérias · Caderno de Erros TDAS</span><span>Snapshot <span data-snapshot></span></span></footer>`}catch(e){setLoadingError(e)}
