import{loadJSON,setupShell,fmtNumber,fmtPct,fmtDate,metric,setLoadingError,routes}from'./common.js?v=26.1';
const BASE='/sedes-tdas-dashboard/';
const completed=value=>/conclu|finaliz|feito|realiz/i.test(String(value||''));
function examCountdown(examDate){const[year,month,day]=examDate.split('-').map(Number),now=new Date();return Math.max(0,Math.ceil((Date.UTC(year,month-1,day)-Date.UTC(now.getFullYear(),now.getMonth(),now.getDate()))/86400000))}
try{
 const d=await loadJSON('data/home.json');setupShell('home',d.meta);const m=d.metrics,days=examCountdown(d.meta.examDate),done=completed(d.today.status),remaining=Math.max(0,Number(m.totalPE||0)-Number(m.completed||0)),primaryHref=done?`${BASE}revisar/?pe=${encodeURIComponent(d.today.pe)}`:`${BASE}estudar/?pe=${encodeURIComponent(d.today.pe)}`,primaryLabel=done?`Revisar ${d.today.pe}`:`Continuar ${d.today.pe}`,statusCopy=done?'Bloco objetivo concluído. A próxima prioridade é consolidar a revisão e preservar o fechamento do ciclo.':`Material e bateria programados para hoje. Continue exatamente do ponto de execução do ${d.today.pe}.`;
 document.querySelector('main').innerHTML=`<section class="hero tdas-home-focus"><div class="tdas-home-focus-head"><span class="kicker">Próximo passo</span><span class="tdas-pe-chip">${d.today.pe}</span></div><h1>${d.today.title}</h1><p class="tdas-home-focus-copy">${statusCopy}</p><div class="hero-actions tdas-home-actions"><a class="btn primary" href="${primaryHref}">${primaryLabel}</a><a class="btn" href="${BASE}resolver/?pe=${encodeURIComponent(d.today.pe)}">Questões</a></div><div class="tdas-home-quick"><span>${d.today.status}</span><span>${d.today.meta} questões</span><span>${d.today.block}</span>${d.today.rd?`<span>${d.today.rd}</span>`:''}</div></section><section class="grid metrics tdas-home-metrics">${metric('Até a prova',fmtNumber(days),`${days===0?'Prova hoje':`${days} dias restantes`} · ${fmtDate(d.meta.examDate)}`)}${metric('PEs restantes',fmtNumber(remaining),`${m.completed} de ${m.totalPE} concluídos`)}${metric('Aproveitamento',fmtPct(m.accuracy),'questões com resultado registrado')}${metric('Hoje',`${fmtNumber(d.today.acertos||0)}/${fmtNumber(d.today.attempted||d.today.meta||0)}`,d.today.action||'Executar próximo passo')}</section><section class="section tdas-home-shortcuts"><div class="section-head"><div><h2>Continuar o ciclo</h2><p>Atalhos essenciais; detalhes técnicos ficam fora da Home.</p></div></div><div class="grid three"><a class="card portal tdas-compact-portal" href="${BASE}revisar/"><small>Praticar</small><b>Revisões</b><span>D+1, D+7, D+20 e reforços pendentes.</span><em>Abrir →</em></a><a class="card portal tdas-compact-portal" href="${routes.redacoes}"><small>Discursiva</small><b>Redações</b><span>Produção, correção e prioridades do Banco Discursivo.</span><em>Abrir →</em></a><a class="card portal tdas-compact-portal" href="${BASE}configuracoes/"><small>Sistema</small><b>Configurações</b><span>Sincronização, PWA, backup, fontes e conforto visual.</span><em>Abrir →</em></a></div></section><footer class="footer"><span>TDAS · Cargo 202</span><span>Última sincronização <span data-sync></span></span></footer>`;
}catch(error){setLoadingError(error)}
