
import{loadJSON,setupShell,fmtDate,fmtNumber,fmtPct,metric,renderBars,escapeHTML,setLoadingError}from'./common.js';
const BASE='/sedes-tdas-dashboard/';
try{const [d,risks,...parts]=await Promise.all([loadJSON('data/subjects.json'),loadJSON('data/risks.json'),loadJSON('data/export/actual-01.json'),loadJSON('data/export/actual-02.json'),loadJSON('data/export/actual-03.json')]);setupShell('riscos',d.meta);const slug=location.pathname.match(/\/materias\/([^/]+)\/?$/)?.[1];const s=d.subjects.find(x=>x.slug===slug);if(!s)throw new Error('Matéria não encontrada.');const recent=risks.recent.filter(x=>x.subject===s.subject),origins=[...new Set(recent.map(x=>x.origin))],actual=parts.flat(),pes=origins.map(id=>actual.find(x=>x.pe===id)).filter(Boolean);document.title=`${s.subject} | TDAS SEDES/DF`;const max=Math.max(1,...s.timeline.map(x=>x.count));document.querySelector('main').innerHTML=`<section class="hero"><span class="kicker">Diagnóstico por matéria</span><h1>${escapeHTML(s.subject)}</h1><p>Volume, reincidência, criticidade, padrões dominantes e próxima ação com base no Caderno de Erros oficial.</p><div class="detail-actions"><a class="btn primary" href="${BASE}riscos/?materia=${encodeURIComponent(s.subject)}">Abrir no Pareto</a><a class="btn" href="${BASE}materias/">Todas as matérias</a></div></section><section class="grid metrics">${metric('Erros',fmtNumber(s.errors),'total catalogado')}${metric('Reincidentes',fmtNumber(s.recurrent),fmtPct(s.errors?s.recurrent/s.errors*100:0,1))}${metric('Altos + críticos',fmtNumber(s.high_critical),fmtPct(s.errors?s.high_critical/s.errors*100:0,1))}${metric('Flashcards',fmtNumber(s.flashcards),`último erro: ${fmtDate(s.latest_date)}`)}</section><section class="section"><div class="grid two"><article class="card panel"><h2>Padrões dominantes</h2><p>Contagem de etiquetas associadas aos erros da matéria.</p><div class="pattern-list">${s.top_patterns.map(p=>`<div class="pattern-row"><span>${escapeHTML(p.pattern)}</span><div class="bar-track"><div class="bar-fill" style="width:${Math.max(4,p.count/s.top_patterns[0].count*100)}%"></div></div><strong>${p.count}</strong></div>`).join('')}</div></article><article class="card formula"><small>Recomendação objetiva</small><strong>${s.errors>=50?'Prioridade máxima':s.errors>=10?'Reforço semanal':'Manutenção dirigida'}</strong><code>${escapeHTML(s.recommendation)}</code></article></div></section><section class="section"><div class="section-head"><div><h2>Ocorrências recentes</h2><p>Quantidade de erros por data nos últimos 30 dias disponíveis.</p></div></div><article class="card panel">${s.timeline.length?`<div class="mini-bars">${s.timeline.map(x=>`<div class="mini-bar" title="${fmtDate(x.date)}: ${x.count}"><i style="height:${Math.max(5,x.count/max*100)}%"></i><small>${x.date.slice(5)}</small></div>`).join('')}</div>`:'<div class="empty">Não houve ocorrência registrada na janela recente.</div>'}</article></section><section class="section"><div class="section-head"><div><h2>Últimos registros públicos</h2><p>Erros recentes desta matéria com acesso ao registro original.</p></div></div>${recent.length?`<div class="table-wrap"><table><thead><tr><th>PE</th><th>Erro</th><th>Gravidade</th></tr></thead><tbody>${recent.map(x=>`<tr><td><a class="linked-id" href="${BASE}pe/${Number(x.origin.replace(/\D/g,''))}/">${escapeHTML(x.origin)}</a></td><td><a href="${x.url}" target="_blank" rel="noopener">${escapeHTML(x.title)} ↗</a></td><td>${escapeHTML(x.severity)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="quality-note">Não há erro individual desta matéria entre os registros recentes expostos pelo snapshot público. Os totais e padrões continuam baseados no banco completo do Notion.</div>'}</section>${pes.length?`<section class="section"><div class="section-head"><div><h2>PE relacionados recentemente</h2><p>Acesso direto aos dias associados aos últimos erros públicos.</p></div></div><div class="grid three">${pes.map(x=>`<a class="card subject-card" href="${BASE}pe/${Number(x.pe.replace(/\D/g,''))}/"><small>${fmtDate(x.date)}</small><b>${x.pe}</b><span>${escapeHTML(x.title)}</span><em>Abrir PE →</em></a>`).join('')}</div></section>`:''}<footer class="footer"><span>${escapeHTML(s.subject)} · diagnóstico consolidado</span><span>Snapshot <span data-snapshot></span></span></footer>`}catch(e){setLoadingError(e)}
